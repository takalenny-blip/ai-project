# 運用ルール

## 目的

このファイルは、`ai-project` における現在の作業手順と横断的な運用ルールを一本化する。日常の作業に組み込み、保存・更新漏れや「保存したつもり」を防ぐ。

また、過去ログに残った「次の作業」を現在の命令として機械的に再実行しない。過去の成功・失敗・判断を現在の作業判断へ反映し、同じ条件で同じ失敗を無意味に繰り返さない。

## 1. 運用フロー（通常時）

1. `BUD.md` を読み、現在の目的・現在状態・現在の次の一手を把握する。
2. 必要に応じて `docs/AI_READ_MAP.md`、`rules/README.md`、`docs/引き継ぎ/現在の引き継ぎ.md`、関係する `projects/` 等を確認する。
3. 過去ログを参照する場合は、事実・過去の判断・過去の計画・完了・失敗／破棄・再試行条件・現在有効なルールを区別する。
4. 作業開始前の現在化・作業判断ゲートを通す。
5. 今回の完了条件と、更新・保存が必要な関連ファイルを先に決める。
6. 作業を実行する。
7. 必要な記録・関連資料をGitHubへ保存する。
8. 保存したファイルを読み戻し、内容・整合性・次回への影響を確認する。
9. 必要ならEXP/REC、引き継ぎ、BUD等を更新し、再度読み戻す。
10. 完了条件を満たしたことを確認して完了とする。

## 2. 運用フロー（過去記録調査）

履歴調査・記事化・経緯確認・漏れ確認など、網羅性が目的の場合は通常の段階読み込みではなく、このフローを適用する。

1. 対象EXPを確定する。
2. GitHub上の実ファイルから、対象EXPに属するRECを全件列挙する。
3. 各RECについて、本文にある一次記録・関連資料への明示参照を確認する。
4. 明示的な直チャット参照がないRECは、発生日、タイトル・テーマ、特徴的な語句、直チャットのファイル名、EXP/REC-ID、関連ファイル名、前後の時系列を組み合わせて候補を探索する。
5. 候補となった直チャットを確認する。必要なら対象RECの前後の時系列も確認する。
6. 特定できない一次記録は推測せず、「特定できなかった」と記録する。
7. Coverageとして、対象EXP、REC件数・ID、確認した直チャット、未特定REC、未読候補と理由、追加調査事項を残す。
8. 次のAIがどこまで調査済みか判断できる状態にして完了とする。

**「READMEに載っている」「主要な3ファイルを読んだ」だけでは網羅調査の完了としない。** READMEは入口であり、調査対象のホワイトリストではない。

## 3. 運用フロー（EXP / REC）

### EXP

EXP-IDを発行 → EXPを作成 → ID管理台帳を更新 → GitHubへ保存 → 読み戻し確認。

### REC

所属EXPを確認 → REC-IDを発行 → RECを作成 → ID管理台帳の最終REC-IDを更新 → GitHubへ保存 → 読み戻し確認。

新規RECを作成するときは、可能な限り関連する `直チャット/` のパスを一次記録としてRECに残す。過去RECの原文を書き換えて出所を補うことはしない。

## 4. 運用フロー（直チャット保存）

直チャットに関する保存ルールは、この節を唯一の正本とする。別ファイルへ同じルールを分散させない。

### 4-1. 保存目的

たか↔バドの会話を、議事録・要約・再構成ではなく、後から検証可能な一次記録としてGitHubへ保存する。

### 4-2. 保存単位

保存は**たかが明示的に保存を指示した場合だけ**行う。

通常の会話は保存対象にしない。

保存ファイル名：`YYYY-MM-DD_直チャット即時保存_連番.md`

連番は当日内の保存順序を正本とする。当日の直チャット保存ファイル群をGitHub上で確認し、同日の最大連番＋1を使用する。日付が変わった初回保存は `001` から開始する。推測で欠番を埋めない。

[User]、[Bud]、ユーザーが貼った[JUN]等を発言者ラベルとして使用する。

### 4-3. 保存タイミング

保存の合図は、たかによる明示的な保存指示とする。例えば「保存して」「やりとり書き込んで」「返答を書き込んで」など。

**保存指示がない限り、各ターンを自動保存してはならない。**

「やりとり書き込んで」は、**その保存合図の言葉を含むUser発言と、その直後に生成するBudの返答**を保存対象とする。保存合図を含むUser発言の前後にある別の発言は対象に含めない。

「返答を書き込んで」は、**その保存合図を含むUser発言の直後に生成するBudの返答だけ**を保存対象とする。そのUser発言自体、保存合図の言葉を含むUser発言、その他の前後の発言は保存対象に含めない。

「直前のやりとり書き込んで」は、保存合図として使用しない。

この保存対象をバドが実際に取得・確認できない場合は、推測・再構成・記憶による補完をしてはならない。「保存対象を取得できないため保存未完了」と明示し、保存しない。

保存対象を確認できた場合は、保存前に対象範囲を簡潔に明示し、たかの意図と違う場合は保存を止める。

保存時は、対象の実際の会話本文を原文として扱う。要約・言い換え・再構成はしない。

### 4-4. 「やりとり書き込んで」後処理

「やりとり書き込んで」は、直チャット一次記録を保存するだけでなく、保存した内容をEXP/RECへ反映すべきかを判定する起点とする。ただし、直チャットの保存単位とEXP/RECの整理単位は別物として扱う。

#### EXP/REC化の判定

「やりとり書き込んで」を受けた場合、バドは**必ず**次の判定を行う。

次のいずれかに該当する場合は、EXP/REC化の対象とする。

- 今回、新しい事実・経験・成功・失敗が確定した。
- 今後の作業で再利用する判断・手順・知見が確定した。
- 既存ルール、運用、設計、作業方法に変更が生じた。
- 問題の原因、再発防止策、再試行条件などが確定した。
- 今回の内容を残さないと、後から同じ判断を再現・検証できなくなる。

次のすべてに該当する場合は、EXP/REC化しない。

- 雑談のみである。
- 検討中で結論が出ていない。
- 一時的な作業メモで、将来の再利用価値がない。
- 既存RECと実質的に同じで、新しい情報がない。

判定が上記のどちらにも明確に該当しない場合は、推測でEXP/REC化せず、**EXP/REC化保留**としてたかへ報告する。

#### インタビューの判定

EXP/REC化の対象と判定した場合、バドは「記録に必要な情報が揃っているか」だけではなく、**たか自身の評価・感想・実感・判断を記録として残す価値があるか**を確認する。

次のようなたか自身の評価・感想・実感・判断が、今回のEXP/RECに残す価値のある情報であり、かつ会話・既存資料から明確に確認できない場合は、たかへのインタビューを行う。

- 「これは面白かった」
- 「これはやってよかった」
- 「ここは失敗だった」
- 「この方法なら今後も使えそう」
- 「このやり方は自分には合わない」
- 「ここは思ったより重要だった」
- 「Junのこの指摘は納得した／納得していない」
- 「今回の結果を見て、今後はこうしたい」

たか自身の評価・感想・実感・判断がすでに会話から確認できる場合は、同じことを確認するためのインタビューを行わない。

たか自身の評価・感想・実感・判断を記録する価値がない場合も、インタビューを行わない。

事実・結果・知見・判断等についても、記録上必要で会話・既存資料から確定できない場合は、推測で補わず、必要な範囲でたかへ確認する。

インタビューは、EXP/REC化対象で、かつ**記録上必要な内容が会話・既存資料から確認できず、その内容をたか本人に確認する必要がある場合にのみ**行う。

インタビューへの回答は、たか本人が追加した評価・感想・実感・判断として扱い、バドが推測した内容をたかの発言として記録してはならない。

**このインタビューは「やりとり書き込んで」を再度発生させる処理ではない。インタビューを行ったことによって、同じ保存・EXP/REC判定・インタビュー判定を最初から繰り返してはならない。**

#### 後処理の順序

「やりとり書き込んで」後は、次の順序で処理する。

1. 直チャット保存・読み戻し確認。
2. EXP/REC化判定。
3. EXP/REC化対象なら、既存EXP/RECとの重複・所属を確認。
4. たか自身の評価・感想・実感・判断を含め、記録上必要な内容を点検する。
5. 記録上必要な内容が会話・既存資料から確認できず、たか本人への確認が必要な場合だけインタビューする。
6. EXP/RECを作成・更新する。
7. EXP/RECのGitHub保存・読み戻し確認。
8. 「EXP/REC化：必要／不要／保留」「インタビュー：実施／不要／未実施（理由）」を含む処理結果をたかへ報告する。

#### 継続作業の状態確認

「やりとり書き込んで」後処理では、EXP/REC判定とは別に、今回の継続作業について現在状態を必ず整理する。

- 確認済み
- 削除済み／変更済み
- 保留
- 失敗・未完了・未確認
- 現在の次の一手

「現在の次の一手」は、過去ログに書かれた予定をそのまま採用せず、今回までに確認できた事実、実施済みの変更、保留、失敗・未完了・未確認事項から現在化して決める。

現在の次の一手を確定できない場合は、推測せず「未確定／要確認」として残す。

この状態確認は、4-4の1回の後処理に含める。状態確認を理由に、同じ保存・EXP/REC判定・インタビュー・状態確認を再帰的に開始してはならない。

**ここまでを1回の「やりとり書き込んで」後処理とし、完了後に同じ処理を再帰的に開始しない。**

**直チャット保存完了だけでは、「やりとり書き込んで」の後処理完了とはしない。**

ただし、「やりとり書き込んで」による直チャット保存そのものの原文範囲は変更しない。EXP/RECへの反映やインタビュー結果を、保存済み直チャットへ後から追記してはならない。

### 4-5. 原文固定

保存対象は実際に会話へ提示した本文そのものとする。

- 要約しない。
- 言い換えない。
- 読みやすく整形し直さない。
- 議事録化しない。
- 「今回の要点」だけに置き換えない。
- 推測した過去発言を補わない。
- ユーザーが貼ったJUN・Claude等の回答は、取得できた提示本文をそのまま保存する。
- 取得できない部分は推測・再構成して補完しない。

### 4-6. 添付ファイル・画像のテキスト化記録

添付ファイルが提示された場合、原則として添付ファイルそのものをGitHubへバイナリ保存するのではなく、「何を読み込み、何が読み取れたか」をテキストとして直チャットの一次記録へ残す。目的は、リポジトリを軽量に保ちながら、後から「AIが何を認識したか」を検証可能にし、画像・文書から得た事実とバドの解釈を混同しないことである。

文書・表計算等からテキストを取得できる場合は、要約ではなく読み取り本文として、取得できた内容を可能な限り原文に近い形で記録する。読み取れない箇所は推測で補完せず、`不明／判読不能`または`読み取り不能`とする。

画像を読み取った場合は、`表示文字`、`画像内容`、`不明／判読不能`を分離する。バドの推測・判断・原因分析は`バドの解釈`として分離する。

「添付ファイル自体を保存した」ことと、「添付内容を読み取りテキストとして一次記録へ保存した」ことは別物として扱う。

### 4-7. 個人情報・秘密情報の保護・マスキング保存

パブリックなGitHubリポジトリを一次記録として運用するため、直チャット保存時に個人情報・秘密情報を保護する。

- 個人情報・秘密情報を検知した場合は警告する。
- GitHubの直チャット一次記録には生の個人情報・秘密情報を保存しない。
- 必要な場合は種別ごとのプレースホルダーへ置換する。
- 電話番号、メールアドレス、住所、本名、パスワード、APIキー、認証トークン、Cookie、セッション情報、カード情報等を保護する。
- IPアドレス、ホスト名、固有のマシン名、アカウント名、ユーザーID等も、必要性がなければ安全側へ倒す。
- 画像・文書等に含まれる保護対象も同様に扱う。

### 4-8. 保存前チェック

保存前に次を確認する。

1. 明示的な保存指示があるか。
2. 保存対象の範囲が実際に取得・確認できているか。
3. [User] / [Bud] / [JUN]等の発言者区分が正しいか。
4. 保存本文が実際の会話本文そのものか。
5. 要約・再構成・追加解釈が混入していないか。
6. 添付の読み取り結果が事実と解釈を分離しているか。
7. 個人情報・秘密情報が必要に応じてマスキングされているか。
8. 連番が既存ファイルと衝突していないか。

### 4-9. 保存後チェック（必須）

保存処理が成功しただけでは完了としない。必ずGitHubから同じファイルを読み戻す。

#### 第1段階：存在確認

- 指定したパスのファイルが存在する。
- ファイル名と連番が正しい。

#### 第2段階：忠実性・安全性確認

読み戻した本文を保存対象本文と照合する。

- 発言者ラベルが一致する。
- 発言順が一致する。
- 文章、記号、改行、コード、箇条書き等に意図しない欠落・追加・言い換えがない。
- ユーザーが貼った他AI回答が省略されていない。
- 添付について記録した読み取り結果が、実際に確認できた内容と一致している。
- 要約文や再構成文になっていない。
- 生の個人情報・秘密情報が残っていない。

保存成功、読み戻し成功、照合成功を実際に確認できない場合は、保存完了と扱わない。

### 4-10. 不一致時の処理

読み戻し結果が保存対象本文と一致しない場合は、保存完了と扱わない。不一致箇所を特定し、現在のファイルとSHAを確認して修正・再保存・再読戻しする。

原文そのものを取得できなくなった場合は、推測で修復しない。「原文取得不能」と明示して停止する。

### 4-11. 失敗時の扱い

- GitHub保存に失敗した場合、保存済みとは扱わない。
- 保存・読み戻し・照合のいずれかに失敗した場合、完了とは扱わない。
- 同日の連番を推測で決めない。
- EXP/RECの保存・読み戻しに失敗した場合も、EXP/REC反映完了とは扱わない。

## 5. 作業開始前の現在化・判断ゲート

作業開始前に、現在の目的・現在状態・現在の次の一手・今回の完了条件・作業対象・更新/保存対象を確認する。

過去資料の「次にやること」は、そのまま現在の命令として扱わず、現在の状況と照合する。

## 6. 終了時チェックリスト（必須）

チャット終了時は、次の順序で確認する。

1. 実績整理
2. 直チャット保存確認
3. 結果・判断
4. 現在資料への反映
5. GitHub保存
6. GitHub読み戻し
7. 次回引き継ぎ確認
8. クローズ判定

### 6-1. 結果・判断

JUN、Claude、その他の外部回答・検証結果がある場合は、その内容を確認したうえで、何を採用するか、保留するか、却下するかを明示する。

**保存完了の報告だけで終了してはならない。**

### 6-2. 直チャット保存後の扱い

「やりとり書き込んで」の保存対象は、4-3に定めるUser発言＋直後のBud返答に固定する。

保存後に行うEXP/REC化判定、インタビュー、結果・判断等は、保存済み直チャットへ後から追記しない。必要な内容はEXP/REC、BUD、引き継ぎ等の現在資料へ反映する。

**保存完了＝終了ではない。**

「やりとり書き込んで」後処理については、4-4の判定・インタビュー・状態確認・保存・読み戻しまでを完了条件とする。

### 6-3. クローズ条件

必要な記録・資料への反映、結果・判断、GitHub保存、読み戻し、関連ファイルの整合性、未完了・未解決事項、今回の完了条件を確認する。

確認できない項目、保存できていない項目、判断できていない項目があれば未完了として明示し、クローズしない。
