# 現在の引き継ぎ

更新日：2026-09-15

## 位置づけ

このファイルは直前セッションの再開用スナップショット。現在の判断は `BUD.md`、行動規則は `rules/運用ルール.md` を優先する。

## 正本

- `takalenny-blip/ai-project`
- 正本識別情報：`docs/正本リポジトリ.md`
- 通常開始：`BUD.md` → `rules/運用ルール.md`

## 現在の作業レーン

**ai-project運用基盤の大手術（一括改修）**。

順序：**現状棚卸し → 大手術の設計 → 採用する改善を確定 → 一括改修 → 検証**。

- 現状棚卸し：完了。
- 大手術の設計：完了。
- 採用する改善：確定。
- 一括改修：実施中。
- 検証：未完了。

たかはClaudeの提案をできるだけ実施する方向を採用。旧連番方式と新タイムスタンプ方式の混在を許容し、一次記録を壊さない。

## 採用する改善

- 現在状態を構造化し、一次記録と現在ビューを分離。
- BUDを現在状態ダッシュボードとして維持し、引き継ぎを現在状態ビューとして扱う。
- 新規直チャットはタイムスタンプ方式を採用可能とし、旧連番ファイルは改名しない。
- ローカルclone＋`git grep` / `rg` を通常検索の第一候補とし、GitHub Contents APIの大量個別取得を通常ルートにしない。
- 保存前・CIの秘密情報検査。
- 命名・重複・リンク・現在状態整合性のCI検査。
- 機械判定可能なルールのスクリプト化。
- Issues / Actionsは必要最小限から整理・活用。

## 今回実施した改修

- `BUD.md` を024まで現在化し、大手術方針・採用改善・現在の次の一手を反映。
- `docs/引き継ぎ/現在の引き継ぎ.md` を024まで現在化。
- `docs/現在状態.json` を新設し、構造化現在状態を固定。
- `docs/大手術設計_2026-09-15.md` を新設。
- 直チャット024を保存・読み戻し確認対象とした。

023同期が前ターンで未完了だった理由は、直チャット保存後に必要なBUD／引き継ぎ現在化・読み戻しまでを実行し切らず、保存処理と保存後処理を混同して止めたバド側の処理漏れ。今回は是正した。

## 直チャット

- 2026-09-15の直近保存済み記録：**024**。
- 実体：`直チャット/2026-09-15_直チャット即時保存_024.md`
- 旧連番と新タイムスタンプ方式は共存可能。旧資産は改名しない。
- 保存後は現在資料へ最新保存情報を同期し、読み戻して一致を確認する。

## 本来のDiMORA工程

大手術完了後に、VAIO P + Chromiumで認証済みDiMORAから機械的データ取得経路を1つ再現可能な形で確立する。

確認済み：Windows + Chromeでは `GL_FAVPGM_DATA.record[]` を確認。「もっと見る」はページ内 `record[]` の追加表示。VAIO P + Firefox ESRでは `GL_FAVPG_DATA` 未確認。長いJavaScript手入力調査は再開しない。家PCは現在の検証環境にしない。

## トラブル対策

VAIO Pローカルclone、`scripts/search_direct_chat.py`、`rules/再開・検索ガード.md` は予備ルート。GitHub Contents API大量個別取得は繰り返さない。

## EXP / REC

既存の `EXP-0000000060 / REC-0000000010` はVAIO P直チャット検索とGitHub APIレート制限、`EXP-0000000070 / REC-0000000011` は状態同期不全・既知事項再調査による停滞を扱う。今回の構造変更は新しい実績を含むため、検証時に重複を確認してEXP/REC化を判断する。

## 未完了

- `rules/運用ルール.md` の保存命名ルールを新旧共存型へ変更。
- タイムスタンプ保存の実装。
- 検索スクリプトのAPI大量取得依存の改修。
- 秘密情報検査の保存前・CI導入。
- 命名・重複・リンク・状態整合性のCI導入。
- 機械的ガード導入。
- EXP/REC一次記録追跡性改善。
- Actions / Issues整理。
- 一括改修後の検証。

DiMORAの具体的方法、認証状態の安全な扱い、取得データ形式・安定性、定期実行等は大手術後に再開・確定する。

## 次の一手

**大手術の残りを一括改修し、最後にBUD・現在の引き継ぎ・`docs/現在状態.json`・一次記録・`rules/運用ルール.md` の整合性を読み戻して検証する。** 検証完了後にDiMORA本来工程へ戻る。

## クローズ判定

- 024保存：完了。
- BUDへの024同期：完了。
- 現在の引き継ぎへの024同期：完了。
- 大手術方針：確定。
- 一括改修：進行中。
- 検証：未完了。
- セッション：作業継続中。
