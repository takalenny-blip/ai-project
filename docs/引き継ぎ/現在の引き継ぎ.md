# 現在の引き継ぎ

更新日：2026-09-10

## 対象

ai-project全体の現在資料整理、AI作業運用、DIGA / DiMORA自動化

## 今回のセッションの目的と完了条件

今回の目的は、2026-09-10のチャットで発生・確認・決定・変更・作成された事項を棚卸しし、実績に応じて直チャット、現在資料、関連プロジェクト資料等へ記録・反映し、GitHub保存後の読み戻しと整合性確認まで完了した状態でセッションをクローズすること。

完了条件は、(1) 今回の実績を一覧化、(2) 各資料の記録・更新要否を判断、(3) 必要な現在資料を更新、(4) 直チャットを1ターン1ファイルで保存、(5) 作成・変更ファイルをGitHubへ保存し読み戻し、(6) 関連ファイル間の矛盾を確認、(7) 未完了事項・未解決問題・次回判断・次回操作を現在化して整理すること。

## 今回の実績

### 新しく発生した事実

- 2026-09-10、2本の音声添付について、音声ファイル自体のバイナリをGitHubへ保存するのではなく、添付情報を直チャット一次記録へ残す方式で処理した。`0035` にファイル名・形式・確認できた再生時間を記録した。
- 2本の音声について、JUNが提示した文字起こし本文がたかから共有され、`0036` に提示された表記どおり保存した。話者は推定せず、すべて`【話者不明】`として記録した。
- 音声そのものをバドが直接検証したとは扱わず、`0036` は「たかが提示したJUNの文字起こし結果」という一次入力として扱った。
- `0037` で、その文字起こし内容とJUNが先に返した4項目の分析を分離して評価した。

### 新しく確認した事実・判断

- 音声1の文字起こしから直接確認できる設計思想は、VAIO Pを「脳」ではなくAIから指示を受けて動く「手足／エッジノード」として使い、重い推論・管理はAI側、VAIO P側はコマンド実行・ローカルスクリプト・DIGA制御等の実務を担わせるという役割分担である。
- 音声2の文字起こしから直接確認できる設計思想は、BUD.mdに現在の目的・状態・次の一手を集約し、過去ログの参照範囲を絞り、現在の方針・状態を過去記録より優先してセッション間の文脈を復旧するという運用である。
- 上記2点はいずれも現在のBUD.mdおよび`rules/運用ルール.md`の方針と整合するため、採用方向と判断した。ただし「採用」は直ちに全面実装する意味ではない。
- VAIO Pのエッジノード化は、現在のDiMORA実証を優先し、段階導入する判断とした。
- JUNの先行分析にあった「デリミタ／物理的隔離」「EXP抽出のバッチ自動化」「開発ログとブログ記事のレイヤー分離」等は、今回提示された2本の文字起こしには直接含まれていない。今回の音声由来の事実としては採用しない。
- したがって、今回の結果によって技術上の現在の次の一手は変更されていない。

### 変更・作成したもの

- `BUD.md`：2026-09-10のセッションで`0035`～`0037`を保存・確認したこと、および今回確認した音声由来の判断を現在状態へ反映した。技術上の次の一手は変更していない。
- `docs/引き継ぎ/現在の引き継ぎ.md`：今回のセッションの実績、資料ごとの更新要否、未完了事項、次回判断を現在化して反映した。
- `直チャット/2026-09-10_直チャット即時保存_0035.md`：保存・読み戻し確認済み。
- `直チャット/2026-09-10_直チャット即時保存_0036.md`：保存・読み戻し確認済み。
- `直チャット/2026-09-10_直チャット即時保存_0037.md`：保存・読み戻し確認済み。
- このクローズターンは、次の連番`0038`として保存する。

## 添付ファイルの扱い

- `VAIO_PをAIの手足にする設計.m4a`：M4A、約11分10.7秒。添付そのもののGitHubバイナリ保存は行っていない。ファイル情報と「当時の環境では音声内容を直接文字起こしできなかった」ことを`0035`に記録し、その後たかから提示されたJUN文字起こしを`0036`に保存した。
- `記憶喪失のAIを動かすBUD.m4a`：M4A、約14分55.8秒。添付そのもののGitHubバイナリ保存は行っていない。扱いは上記と同じ。
- `0036`に保存した音声本文は、バドが音声を直接聞き取った結果ではなく、たかが提示したJUNの文字起こし結果である。
- 添付そのものを保存したことと、添付から得られた情報をテキスト一次記録へ保存したことは別物として扱う。

## 資料ごとの記録・更新判断

| 資料 | 今回の実績を記録する必要 | 現在内容の変更 | 判断 |
|---|---|---|---|
| `直チャット/` | 必須 | 必要 | `0035`～`0038`を今回の一次記録として1ターン1ファイルで保存し、GitHub読み戻しで存在・原文忠実性を確認する。`0010`は欠番として推測で埋めず、`0017`の旧原文忠実性問題も維持する。 |
| `rules/運用ルール.md` | 必要 | 不要 | 現行ルールで今回の添付記録、事実と解釈の分離、直チャット保存、GitHub読み戻しを処理できる。今回の実績によるルール変更はない。 |
| `rules/直チャット保存プロトコル.md` | 確認 | 不要 | 当該パスはGitHub上で現在確認できず、現行の正本は`rules/運用ルール.md`の直チャット保存節として扱う。新規作成・復元は今回の実績からは行わない。 |
| `BUD.md` | 必要 | 必要 | 直チャットの現在保存状況を`0037`まで反映し、音声から直接確認した設計思想と採用方向を現在状態へ反映した。技術上の次の一手は変更しない。 |
| `docs/引き継ぎ/現在の引き継ぎ.md` | 必須 | 必要 | 今回のセッションの現在状態・判断・未完了事項を次回再開可能な形へ更新した。 |
| `projects/DIGA自動化.md` | 確認 | 不要 | 今回の音声評価は既存のDIGA/DiMORA方向と整合し、技術上の現在状態・具体的取得方式を変更していないため、過去プロジェクト資料を書き換えない。 |
| `docs/AI_READ_MAP.md` | 確認 | 不要 | 読解順・正本関係は変わっていない。 |
| `projects/README.md` | 不要 | 不要 | 今回の実績を入口資料へ追加する必要はない。 |
| `rules/README.md` | 不要 | 不要 | 現行ルール入口として矛盾なし。 |
| `README.md` | 不要 | 不要 | 現在の基本方針と矛盾なし。 |
| `指針等/ID管理台帳.md` | 不要 | 不要 | 今回は新規EXP/RECを作成・更新しないため、ID発行なし。 |
| `経験ログ/EXP-0000000040.md` | 不要 | 不要 | 今回は既存EXPの現在内容を書き換える根拠がない。 |
| `経験ログ/REC_EXP-0000000040_REC-0000000020.md` | 不要 | 不要 | 当時の記録として保持し、今回の現在事実で過去記録を書き換えない。 |
| その他の過去資料 | 原則不要 | 不要 | 今回の実績で現在の目的・状態・判断・運用ルールが変わらないため、機械的に更新しない。 |

## EXP / REC / ID台帳

今回のセッションでは新規EXP/RECの作成・更新は行わない。したがってID発行・ID管理台帳更新・EXP/RECの参照関係更新は不要。

今回の音声内容は既存のVAIO P / DIGA自動化・BUD運用方針の確認と判断整理であり、現在の技術状態を新しい経験DB記録へ昇格させる必要性は現時点では低いと判断した。必要性が後から生じた場合は、その時点の現行仕様に従って新規EXP/RECを発行する。

## 現在の技術上の未完了・未解決

- Firefox ESR上で `GL_FAVPGM_DATA.record[]` が存在するか未確認。
- Firefox ESRから必要データを安全に取得する具体的方法は未確定。
- 認証状態を安全に扱う方法は未確定。
- 取得処理をVAIO Pへ移す具体的方法は未確定。
- 定期実行、変更検知、機種別制御、警告メールまで含めた現行仕様は未確定。
- Firefox ESR / VAIO Pでの性能・安定性は未確認。

## 今回の音声評価に関する未完了・未解決

- 2本の音声自体をバドが直接聞き取って検証したわけではない。今回の内容評価は、たかから提示されたJUNの文字起こしを一次入力として行った。
- JUNの文字起こしが音声と完全一致するかは、今回のセッションでは独立検証していない。
- 音声に含まれていない具体策をJUNの分析から遡って音声由来とみなさない、という出所分離は完了した。

## 次回に引き継ぐべき事実・判断

- 次回の技術作業は、過去ログの「次にやること」ではなく、BUD.mdの現在の次の一手を基準に判断する。
- 現時点の最優先は、VAIO P + Firefox ESRで認証済みDiMORAページを開き、`GL_FAVPGM_DATA.record[]` の存在を小さく確認すること。
- Windows + Chromeでの確認結果をFirefox ESRへそのまま外挿しない。
- 2026-09-08の制限付きWindows PCではF12、Ctrl + Shift + I、Ctrl + U、ChromeメニューからのDevTools操作が利用できなかったため、同じ条件で再試行しない。
- Firefox ESRでの存在確認前にChrome Remote Debuggingを本番方式として実装しない。
- VAIO PはAIの「脳」として重い推論を担わせるのではなく、AI側と役割分担するエッジノード／手足として段階的に使う設計思想を採用方向とする。ただし、具体的な分散アーキテクチャは現時点で確定していない。
- BUD.mdを現在の目的・状態・次の一手の復旧中心とし、過去ログの参照範囲を必要最小限にするという思想は、現行運用と整合するため継続する。
- `0010`は欠番として推測で埋めない。`0017`は原文忠実性に問題がある旧保存として注意する。
- 直チャットは今後も1ターン1ファイル、即時保存、GitHub読み戻し、存在確認＋原文忠実性確認を行う。
- 添付を扱った場合は、添付そのものの保存有無と、読み取った情報のテキスト一次記録化を分離し、読み取れない内容を推測しない。
- JUNの追加分析のうち、今回の文字起こしに直接含まれていない具体策は音声由来の事実として扱わない。

## 今回のクローズ時点

- 今回の実績一覧化：完了。
- 資料ごとの記録・更新要否判断：完了。
- `BUD.md`更新：完了、GitHub保存済み。読み戻し確認が必要。
- `docs/引き継ぎ/現在の引き継ぎ.md`更新：完了、GitHub保存済み。読み戻し確認が必要。
- `0035`～`0037`：GitHub保存・読み戻し確認済み。
- `0038`：このクローズターンの最終本文を保存・読み戻し確認する。
- EXP/REC：変更なし。
- ID管理台帳：変更なし。
- `projects/DIGA自動化.md`：変更なし。
- 技術作業：未着手。次回判断へ引き継ぐ。
