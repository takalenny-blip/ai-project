# 現在の引き継ぎ

更新日：2026-09-09

## 対象

ai-project全体の現在資料整理、AI作業運用、DIGA / DiMORA自動化

## 今回の作業と完了条件

今回の目的は、現在状態をBUDへ集約し、運用フローと横断ルールを `rules/運用ルール.md` に一本化して、入口ファイルと正本をコンパクトにすること、ならびにAIとのセッション開始・終了時の依頼文を現在の運用方針に合わせて明確化すること。

完了条件は、(1) BUDへ現在状態・現在の次の一手を統合、(2) 運用フローを一本化、(3) 通常時・過去記録調査・EXP/REC・直チャット保存・GitHub保存・引き継ぎを「運用フロー」として整理、(4) ルール・禁止事項をフローと区別、(5) README類・引き継ぎの参照先を更新、(6) 旧CURRENT_STATEと旧2フローを削除し参照を整理、(7) GitHub保存後に読み戻し確認、(8) 始まり・終わりの依頼文で「必要な／必要なら」等の判断基準が曖昧な表現を条件・手順へ置き換えること。

## 今回やったこと

- `rules/運用ルール.md` を新規作成し、運用フローとルールを一本化した。
- フロー名は「運用フロー（通常時）」「運用フロー（過去記録調査）」「運用フロー（EXP / REC）」「運用フロー（直チャット保存）」「運用フロー（GitHub保存・読み戻し）」「運用フロー（セッション終了・引き継ぎ）」とした。
- 「失敗・再試行ルール」「記録ルール」「禁止事項」は、運用フローではないためその名称を明確に分離した。
- `BUD.md` に現在状態、現在の次の一手、VAIO P / DIGA / 経験DB / 直チャットの現状を集約した。
- `docs/AI_READ_MAP.md` をBUD正本・運用ルール一本化後の読解順へ更新した。
- `rules/README.md` を正本への入口として簡素化した。
- ルート `README.md` の読解順と参照先を更新した。
- 旧 `docs/00_CURRENT_STATE.md`、旧 `rules/AI作業運用フロー.md`、旧 `rules/過去記録調査・追跡フロー.md` を削除した。
- バド用の始まりの言葉を、読む資料・過去記録へ進む条件・作業開始前の確認・保存後の検証まで明示する形に修正した。
- バド用の終わりの言葉を、今回の実績の棚卸し→資料ごとの更新要否判断→保存→読み戻し→整合性確認→引き継ぎ→完了判定という順序で明示する形に修正した。
- JUN用の始まりの言葉も、現在資料を基準とし、判断に不足がある場合だけ調査範囲を広げる条件を明示する形に修正した。

## 確定

- **BUD.mdが現在の目的・現在状態・現在の次の一手の正本。**
- **`rules/運用ルール.md` が作業手順・横断的な運用ルールの正本。**
- 「運用フロー」と「ルール」を名称・役割の両面で区別する。
- `docs/AI_READ_MAP.md` と `rules/README.md` は案内・入口であり、現在状態の正本を持たない。
- 過去ログの「次にやること」は現在の命令ではない。
- 直チャットは1ターン1ファイルで原文保存し、ユーザーが貼った他AI回答も完全な提示原文として保存する。
- 網羅性が目的の履歴調査では、対象EXPのREC全件列挙、出所追跡、Coverage確認まで行う。
- セッション開始時は、BUD.md→rules/運用ルール.mdを基準に読み、そこで判断できない事項がある場合だけ該当する現在資料へ進み、過去記録は現在資料で判断できず過去の経緯等が判断に関係する場合に追跡する。
- セッション終了時は、今回の実績を棚卸しし、実績を根拠に各資料の記録・更新要否を判断し、保存・読み戻し・整合性確認・引き継ぎ確認まで行って完了判定する。

## 現在の技術上の次の一手

BUD.mdに記載した通り、VAIO P + Firefox ESRで認証済みDiMORAページを開き、`GL_FAVPGM_DATA.record[]` が存在するかを小さく確認すること。

## 未確定

- Firefox ESR上で `GL_FAVPGM_DATA.record[]` が存在するか。
- Firefox ESRから必要データを安全に取得する具体的方法。
- 認証状態を安全に扱う方法。
- 取得処理をVAIO Pへ移す具体的方法。
- 定期実行、変更検知、機種別制御、警告メールまで含めた現行仕様。
- Firefox ESR / VAIO Pでの性能・安定性。

## 過去の技術上の注意

2026-09-08、制限付きWindows PCではF12、Ctrl + Shift + I、Ctrl + U、ChromeメニューからのDevTools操作が利用できなかった。同じ条件で無意味に再試行しない。

Windows + Chromeで確認済みだからFirefox ESRでも同じ、と推測しない。Firefox ESRでの存在確認前にChrome Remote Debuggingを本番方式として実装しない。

## 直チャット保存状況

0009まで保存確認済み。0010は保存処理が失敗しており未保存。0011まで保存確認済み。欠番を推測で埋めない。原文を確認できる場合だけ正しく補完・保存する。

## 旧ファイルの扱い

BUDへの現在状態統合、`rules/運用ルール.md` 作成、入口・引き継ぎ資料の更新、旧3ファイルの削除まで完了した。旧ファイルの削除はGitHub上のツリーで確認済み。

次の技術作業は、BUD.mdに記載した現在の次の一手から再開する。