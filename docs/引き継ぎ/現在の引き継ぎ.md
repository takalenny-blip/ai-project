# 現在の引き継ぎ

更新日：2026-09-15

## 位置づけ

このファイルは直前セッションの再開用スナップショット。最終形では `docs/現在状態.json` から生成されるビューとする。現在の判断は `BUD.md`、行動規則は `rules/運用ルール.md` を優先する。

## 正本

- `takalenny-blip/ai-project`
- 正本識別情報：`docs/正本リポジトリ.md`
- 通常開始：`BUD.md` → `rules/運用ルール.md`
- 現在状態の最終的な唯一の正本：`docs/現在状態.json`（自動生成移行を実装中。現在は移行期間）

## 現在の作業レーン

**ai-project運用基盤の大手術**。

ここでいう「一括改修」は、設計を一括で行うことを意味し、9項目をまとめて変更して最後に検証することを意味しない。実装は**変更 → 読み戻し → 機械判定可能な合格基準確認 → 次へ**の小さな検証単位で進める。

順序：**現状棚卸し → 大手術の設計 → 採用する改善を確定 → 小さな検証単位で順次実装 → 検証**。

- 現状棚卸し：完了。
- 大手術の設計：Claude追加提案を反映して改訂済み。
- 採用する改善：確定。
- 自動生成の実装：未着手。
- CIガード：未着手。
- タイムスタンプ方式：未着手。
- その他改修：未着手。
- 検証：未完了。

## 最優先で固める設計

`docs/現在状態.json` → 生成スクリプト → BUD/引き継ぎ → CI/Actions、という一方向フローを基本とする。

先に確定するもの：

1. 生成スクリプトの実装主体。
2. スクリプトの置き場所と実行環境。
3. GitHub Actions等の実行トリガー。
4. 生成失敗時の扱い。中途半端なBUD/引き継ぎを残さないこと。
5. 自動生成が完成するまでの移行期間の扱い。
6. 各工程の機械判定可能な合格基準。

## 採用する改善

- 現在状態を構造化し、一次記録と現在ビューを分離。
- `docs/現在状態.json`を唯一の現在状態正本とし、BUD/引き継ぎを自動生成ビューにする。
- 最新直チャットへの単一パスを現在状態に保持し、命名規則から最新判定を切り離す。
- 新規直チャットはタイムスタンプ方式へ移行可能とし、旧連番ファイルは改名しない。
- ローカルclone＋`git grep` / `rg` を通常検索の第一候補とし、GitHub Contents APIの大量個別取得を通常ルートにしない。
- 保存前・CIの秘密情報検査。
- 命名・重複・リンク・現在状態整合性のCI検査。
- 機械判定可能なルールのスクリプト化。
- Issues / Actionsは必要最小限から整理・活用。

## 今回の保存・現在化

- 直チャット026を保存した。
- `docs/現在状態.json` を026と改訂設計に合わせて更新した。
- `docs/大手術設計_2026-09-15.md` にClaude追加3点（実装主体、移行期間、合格基準）と小単位実装を反映した。
- BUDを026まで現在化した。
- この引き継ぎも026まで現在化した。

## 直チャット

- 2026-09-15の直近保存済み記録：**026**。
- 実体：`直チャット/2026-09-15_直チャット即時保存_026.md`
- 旧連番と新タイムスタンプ方式は共存可能。旧資産は改名しない。
- 現在状態JSONに最新直チャットの単一パスを持たせる。
- 自動生成移行完了までは現行保存後処理を行うが、手動二重管理を恒久運用にはしない。

## 本来のDiMORA工程

大手術完了後に、VAIO P + Chromiumで認証済みDiMORAから機械的データ取得経路を1つ再現可能な形で確立する。

確認済み：Windows + Chromeでは `GL_FAVPGM_DATA.record[]` を確認。「もっと見る」はページ内 `record[]` の追加表示。VAIO P + Firefox ESRでは `GL_FAVPGM_DATA` 未確認。長いJavaScript手入力調査は再開しない。家PCは現在の検証環境にしない。

## トラブル対策

VAIO Pローカルclone、`scripts/search_direct_chat.py`、`rules/再開・検索ガード.md` は予備ルート。GitHub Contents API大量個別取得は繰り返さない。

## EXP / REC

既存の `EXP-0000000060 / REC-0000000010` はVAIO P直チャット検索とGitHub APIレート制限、`EXP-0000000070 / REC-0000000011` は状態同期不全・既知事項再調査による停滞を扱う。今回の構造変更は新しい実績を含むため、検証時に重複を確認してEXP/REC化を判断する。

## 未完了

- 自動生成の実装主体・実行場所・トリガー・失敗時の扱いの確定。
- 移行期間の運用方針の確定。
- 各工程の機械判定可能な合格基準の確定・実装。
- `docs/現在状態.json` → BUD/引き継ぎ生成スクリプトの実装。
- CIによる機械的ガードの先行導入。
- `rules/運用ルール.md` の保存命名ルールを新旧共存型へ変更。
- タイムスタンプ保存の実装。
- 検索スクリプトのAPI大量取得依存の改修。
- 秘密情報検査の保存前・CI導入。
- EXP/REC一次記録追跡性改善。
- Actions / Issues整理。
- 一括改修後の検証。

DiMORAの具体的方法、認証状態の安全な扱い、取得データ形式・安定性、定期実行等は大手術後に再開・確定する。

## 次の一手

**まず「現在状態.json → 生成スクリプト → BUD/引き継ぎ → CI/Actions」の一方向フローについて、実装主体・実行場所・トリガー・失敗時の扱い・移行期間・各工程の合格基準を確定する。次に最初の小さな変更を行い、GitHubから読み戻して合格基準を機械的に確認し、その確認後に次へ進む。**

## クローズ判定

- 026保存：完了。
- `docs/現在状態.json`への026反映：完了。
- BUDへの026同期：完了。
- 現在の引き継ぎへの026同期：完了。
- 大手術設計の改訂：完了。
- 自動生成・CI等の実装：未着手。
- 大手術：進行中。
- 検証：未完了。
- セッション：作業継続中。
