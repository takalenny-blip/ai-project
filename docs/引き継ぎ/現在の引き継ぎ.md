# 現在の引き継ぎ

更新日：2026-09-15

## 位置づけ

このファイルは直前セッションの再開用スナップショット。最終形では `docs/現在状態.json` から生成されるビューとする。現在の判断は `BUD.md`、行動規則は `rules/運用ルール.md` を優先する。

## 正本

- `takalenny-blip/ai-project`
- 正本識別情報：`docs/正本リポジトリ.md`
- 通常開始：`BUD.md` → `rules/運用ルール.md`
- 現在状態の最終的な唯一の正本：`docs/現在状態.json`（自動生成移行中）

## 現在の作業レーン

**ai-project運用基盤の大手術**。

ここでいう「一括改修」は、設計を一括で行うことを意味し、9項目をまとめて変更して最後に検証することを意味しない。実装は**変更 → 読み戻し → 機械判定可能な合格基準確認 → 次へ**の小さな検証単位で進める。

順序：**現状棚卸し → 大手術の設計 → 採用する改善を確定 → 小さな検証単位で順次実装 → 検証**。

- 現状棚卸し：完了。
- 大手術の設計：Claude追加提案を反映して改訂済み。
- 採用する改善：確定。
- 第1実装：生成スクリプト＋Actionsによる決定性検証を追加済み。
- CI実行：`render-and-check` が失敗。失敗ステップは特定済み、詳細ログの取得待ち。
- CIガード本体：未着手。
- タイムスタンプ方式：未着手。
- その他改修：未着手。
- 検証：詳細ログ取得→失敗原因確認→最小修正→再検証。

## 最優先で固める設計

`docs/現在状態.json` → 生成スクリプト → BUD/引き継ぎ → CI/Actions、という一方向フローを基本とする。

第1実装として、`scripts/generate_current_views.py` と `.github/workflows/current-state-views.yml` を追加した。スクリプトは現在状態JSONからBUD/引き継ぎ候補を生成し、Actionsは同一入力から同一出力になることを検証する。

移行中なので、既存BUD/引き継ぎを自動上書き・自動コミットしない。これにより既存資料を壊さず、生成器を先に検証できる。

## ループ判定

**ループしていない。** 今回は設計説明の繰り返しではなく、第1実装まで進め、その実行時にCI失敗が確認された。現在は失敗原因の特定という実装検証段階。

Actionsのpushトリガーは `docs/現在状態.json`、生成スクリプト、workflow定義の変更に限定し、生成候補をコミットしないため、生成処理自身による自己再トリガー構造にはしていない。

## 今回の保存・現在化

- 直チャット030を保存した。
- `docs/現在状態.json` を030、`render-and-check` の失敗ステップ特定、詳細ログ取得待ちへ現在化した。
- BUDを030と同状態へ現在化した。
- この引き継ぎも030と同状態へ現在化した。
- 029時点のActions失敗について、Node.js 20表示は警告であり、主原因とは未確定とした。
- GitHub画面で `Verify deterministic render` をクリックしてもログが展開されない場合があるため、たかに無理な画面操作を続けさせず、詳細ログを取得できる経路をこちらで確認する方針に訂正した。

## 直チャット

- 2026-09-15の直近保存済み記録：**030**。
- 実体：`直チャット/2026-09-15_直チャット即時保存_030.md`
- 旧連番と新タイムスタンプ方式は共存可能。旧資産は改名しない。
- 現在状態JSONに最新直チャットの単一パスを持たせる。
- 自動生成移行完了までは現行保存後処理を行うが、手動二重管理を恒久運用にはしない。

## 本来のDiMORA工程

大手術完了後に、VAIO P + Chromiumで認証済みDiMORAから機械的データ取得経路を1つ再現可能な形で確立する。

確認済み：Windows + Chromeでは `GL_FAVPGM_DATA.record[]` を確認。「もっと見る」はページ内 `record[]` の追加表示。VAIO P + Firefox ESRでは `GL_FAVPGM_DATA` 未確認。長いJavaScript手入力調査は再開しない。家PCは現在の検証環境にしない。

## トラブル対策

VAIO Pローカルclone、`scripts/search_direct_chat.py`、`rules/再開・検索ガード.md` は予備ルート。GitHub Contents API大量個別取得は繰り返さない。

## EXP / REC

既存の `EXP-0000000060 / REC-0000000010` はVAIO P直チャット検索とGitHub APIレート制限、`EXP-0000000070 / REC-0000000011` は状態同期不全・既知事項再調査による停滞を扱う。今回の構造変更は新しい実績を含むため、検証時に重複を確認してEXP/REC化を判断する。

## 未完了

- `render-and-check` の詳細ログ取得経路確認と失敗原因特定、最小修正・再検証。
- 自動生成への完全移行方法と失敗時の原子性の確定。
- CIによる機械的ガード本体の導入。
- `rules/運用ルール.md` の保存命名ルールを新旧共存型へ変更。
- タイムスタンプ保存の実装。
- 検索スクリプトのAPI大量取得依存の改修。
- 秘密情報検査の保存前・CI導入。
- EXP/REC一次記録追跡性改善。
- Actions / Issues整理。
- 一括改修後の検証。

DiMORAの具体的方法、認証状態の安全な扱い、取得データ形式・安定性、定期実行等は大手術後に再開・確定する。

## 次の一手

**GitHub Actionsの `render-and-check` の詳細ログを取得できる経路を確認する。原因が分かったら、最小修正→読み戻し→再検証を行う。合格なら、既存BUD/引き継ぎを完全生成ビューへ移行する小単位を設計する。**

## クローズ判定

- 027保存：完了。
- 028保存：完了。
- 029保存：完了。
- 030保存：完了。
- `docs/現在状態.json`への030反映：完了。
- BUDへの030同期：完了。
- 現在の引き継ぎへの030同期：完了。
- 大手術設計の改訂：完了。
- 第1実装：完了。
- 第1実装CI：失敗。失敗ステップは特定済み、詳細ログと原因分析待ち。
- 自動生成・CIガード本体等：未完了。
- 大手術：進行中。
- 検証：詳細ログ取得待ち。
- セッション：作業継続中。
