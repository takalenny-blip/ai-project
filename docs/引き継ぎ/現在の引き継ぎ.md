# 現在の引き継ぎ

更新日：2026-09-10

## 対象

ai-project全体の現在資料整理、AI作業運用、DIGA / DiMORA自動化

## 今回のセッション結果

2026-09-10の今回のセッションでは、2本の音声について、たかから提示されたJUNの文字起こしを一次入力として内容を確認し、バドが設計上の意味を評価した。その後、JUNに現在の `BUD.md` / `rules/運用ルール.md` との整合性を確認してもらった。

JUNの確認結果は、現在のプロジェクト定義・運用ルールを変更する実質的な差分はない、という第三者的なバリデーションだった。

今回確認された判断は次のとおり。

- 「過去は豊かに保存する。ただし通常の作業では現在状態を中心にし、必要なときだけ過去を取り出す」という設計思想は、既存の `BUD.md` / `rules/運用ルール.md` と整合する。
- VAIO PをAIの「手足／エッジノード」として使う設計思想は既存方針と整合し、段階導入の方向で継続する。
- 経験ログAI下書き、ブログ編集レイヤー、ディレクトリ大改造は現時点では保留または不要。現在の最優先作業には影響させない。
- 今回の確認によって、技術上の現在の次の一手は変更されていない。

## 直チャット保存

今回の関連ターンは、`0035`～`0039`までGitHub上で保存・読み戻し確認済み。今回のクローズターンは次の連番 `0040` とする。

`0010` は既知の保存失敗による欠番であり、推測で埋めない。`0017`には以前確認された原文忠実性の問題があるため、旧保存を無条件に正本とは扱わない。

`0039`は、たかが提示したJUNの整合性確認結果と、たかの「直チャットは書き込んでいるよね？」という確認、およびバドの回答を含む記録であり、GitHubから読み戻して内容を確認済み。

なお、`0035`～`0037`については、ファイルの存在・読み戻しは確認済みだが、現在の厳格な「実際に会話へ提示した最終本文そのもの」という原文忠実性まで完全に証明できない部分がある。特に音声文字起こし記録は整理形式で保存されており、当時の実際の提示本文との完全一致を独立には証明できない。この不確実性は隠さず引き継ぐ。

## 添付ファイルの扱い

- `VAIO_PをAIの手足にする設計.m4a`：添付そのものをGitHubへバイナリ保存していない。ファイル情報を直チャット一次記録へ保存した。
- `記憶喪失のAIを動かすBUD.m4a`：添付そのものをGitHubへバイナリ保存していない。ファイル情報を直チャット一次記録へ保存した。
- 音声本文として扱った内容は、バドが音声を直接聞き取った結果ではなく、たかから提示されたJUNの文字起こしである。
- JUNの文字起こしと音声そのものとの完全一致は独立検証していない。
- 音声由来の事実と、JUNの追加分析・バドの解釈は分離して扱う。

## 資料ごとの更新判断

| 資料 | 記録必要 | 現在内容の変更 | 判断 |
|---|---|---|---|
| `直チャット/` | 必須 | 必要 | `0035`～`0039`を保存済み。今回のクローズターンを`0040`として保存し、読み戻し・原文忠実性を確認する。 |
| `BUD.md` | 必要 | 必要 | 直チャットの現在保存状況とJUNによる第三者的バリデーションを現在状態へ反映済み。技術上の次の一手は変更していない。 |
| `docs/引き継ぎ/現在の引き継ぎ.md` | 必須 | 必要 | 今回の判断、保存状態、未完了事項、次回の現在判断を反映済み。 |
| `rules/運用ルール.md` | 確認 | 不要 | 現行ルールで今回の保存・添付記録・事実と解釈の分離を処理できる。ルール変更なし。 |
| `rules/直チャット保存プロトコル.md` | 確認 | 不要 | 現在確認できる正本は `rules/運用ルール.md` の直チャット保存節。今回新規作成・復元しない。 |
| `projects/DIGA自動化.md` | 確認 | 不要 | 技術上の現在状態・取得方式に変更なし。 |
| `docs/AI_READ_MAP.md` | 確認 | 不要 | 読解順・正本関係に変更なし。 |
| `projects/README.md` | 不要 | 不要 | 今回追加不要。 |
| `rules/README.md` | 不要 | 不要 | 今回追加不要。 |
| `README.md` | 不要 | 不要 | 今回追加不要。 |
| `指針等/ID管理台帳.md` | 不要 | 不要 | EXP/RECの作成・更新なし。ID発行なし。 |
| 既存EXP/REC | 原則不要 | 不要 | 今回の実績で過去記録を書き換える根拠なし。 |

## EXP / REC

今回、新規EXP/RECは作成・更新しない。したがってID管理台帳、参照関係も変更しない。

## 現在の技術上の未完了・未解決

- VAIO P + Firefox ESRで認証済みDiMORAページを開き、`GL_FAVPGM_DATA.record[]` が存在するか未確認。
- Firefox ESRから必要データを安全に取得する具体的方法は未確定。
- 認証状態を安全に扱う方法は未確定。
- 取得処理をVAIO Pへ移す具体的方法は未確定。
- 定期実行、変更検知、機種別制御、警告メールまで含めた現行仕様は未確定。
- Firefox ESR / VAIO Pでの性能・安定性は未確認。

## 今回の音声評価に関する未完了・未解決

- 2本の音声自体をバドが直接聞き取って検証したわけではない。
- JUNの文字起こしが音声と完全一致するかは独立検証していない。
- 音声内に直接含まれない具体策を、音声由来の事実として採用しないという出所分離は完了。

## 次回に引き継ぐべき現在判断

- 次回は過去資料の「次にやること」ではなく、現在の `BUD.md` を基準に作業判断する。
- 最優先はVAIO P + Firefox ESRでの `GL_FAVPGM_DATA.record[]` 存在確認。
- Windows + Chromeの確認結果をFirefox ESRへそのまま外挿しない。
- 制限付きWindows PCで利用できなかったDevTools操作を同じ条件で再試行しない。
- Firefox ESRでの存在確認前にChrome Remote Debuggingを本番方式として実装しない。
- VAIO PはAIの「脳」ではなく、AI側と役割分担するエッジノード／手足として段階的に使う方向。ただし具体的な分散アーキテクチャは未確定。
- 「保存は豊かに・通常参照は絞る」は既存方針の第三者的バリデーションとして扱い、今回これを新規ルール化しない。
