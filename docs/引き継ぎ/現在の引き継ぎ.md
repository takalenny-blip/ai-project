# 現在の引き継ぎ

更新日：2026-09-15

## 位置づけ

このファイルは**直前セッションの再開用スナップショット**であり、現在状態・現在の次の一手の独立した正本ではない。現在の判断は `BUD.md`、行動規則は `rules/運用ルール.md` を優先する。

## 正本リポジトリ

- **`takalenny-blip/ai-project`**
- 正本識別情報：`docs/正本リポジトリ.md`
- 通常開始時は、このリポジトリの `BUD.md` → `rules/運用ルール.md` を確認する。

## 対象

ai-project全体の現在資料整理、AI作業運用、DIGA / DiMORA自動化、VAIO P/Chromium実用確認

## 現在の全体進行

大枠は次の順序で管理する。

1. 基盤を整える
2. 実験環境を整える
3. 自動化対象を一つずつ実証する
4. 実証済み処理を実用化する
5. 経験をEXP/RECへ資産化する
6. 経験から記事素材を抽出・文章化する
7. Blogger等へつなげる

各作業は **①目的 → ②完了条件 → ③現状 → ④次の一手 → ⑤実行 → ⑥完了確認・記録** で管理する。既に完了した確認を、別の理由なく繰り返さない。

## 現在の作業レーン

**今のレーン：ai-project運用基盤の大手術（一括改修）。**

2026-09-15、たかはClaudeの提案をできるだけ実施する方向を採用し、旧連番方式と新タイムスタンプ方式の混在を許容し、運用全体を大きく改修する方針を確定した。

### 大手術の順序

**現状棚卸し → 大手術の設計 → 採用する改善を確定 → 一括改修 → 検証**

現状棚卸し：完了。

大手術の設計：完了。主対象は、現在状態の単一化、一次記録と現在ビューの分離、直チャット命名、検索経路、秘密情報保護、機械的ガード、CI、EXP/REC追跡性、Issues/Actions整理。

採用する改善：確定。一次記録を壊さず、旧連番ファイルを改名せず、新方式を追加できる互換型で進める。

一括改修：実施中。

検証：一括改修完了後に実施する。

### 採用する改善

- 現在状態を構造化し、一次記録と現在ビューを分離する。
- BUDを現在状態のダッシュボードとして維持し、引き継ぎを現在状態の自動生成対象として扱う。
- 新規直チャット保存はタイムスタンプ方式を採用可能とし、旧連番ファイルを移行・改名しない。
- ローカルclone＋`git grep` / `rg` を通常検索の第一候補とし、GitHub Contents APIの大量個別取得を通常ルートにしない。
- 秘密情報検査を保存前・CIの両方へ導入する。
- 命名・重複・リンク・現在状態整合性をCIで検査する。
- ルールのうち機械判定可能なものをスクリプト化し、判断が必要な部分は人間／AI判断として残す。
- Issues / Actionsは必要最小限から整理・活用する。

## 本来のDiMORA工程

大手術完了後の通常レーンは、**VAIO P + Chromiumで、認証済みDiMORAページから機械的にデータを取得できる経路を1つ再現可能な形で確立すること**。

**完了条件：** 認証済みDiMORA上で取得経路を実証し、再現可能性を確認すること。

確認済みの基準情報：
- Windows + Chromeの認証済みDiMORAでは `GL_FAVPGM_DATA.record[]` を確認済み。
- 「もっと見る」はページ内 `record[]` の追加表示であることを確認済み。
- VAIO P + Firefox ESRでは `GL_FAVPG_DATA` が見つからなかった。
- Firefox ESRでの長いJavaScript手入力による細かなConsole調査は非効率として打ち切り、同じ方法を繰り返さない。
- 家PCをDiMORA取得方式の現在の検証環境にはしない。

## トラブル対策（予備ルート）

本来の工程とは別枠で管理する。

- VAIO Pローカルclone：GitHub検索/APIに具体的な問題がある場合の予備経路。
- `scripts/search_direct_chat.py`：GitHub APIレート制限を確認済みのため、同じAPI方式を通常ルートとして繰り返さない。
- `rules/再開・検索ガード.md`：検索失敗をデータ不存在と扱わず、既知事項をリセットしないための補助ルール。

## 一次記録検索の状態

- `scripts/search_direct_chat.py` はVAIO P上で実行できた。
- `python3 scripts/search_direct_chat.py chromium` の初回実行では直チャット内容が実際に出力された。
- 再実行では `403 rate limit exceeded` が発生した。
- 原因はGitHub Contents APIで多数のファイルを個別取得する実装による未認証レート制限と判断。
- 同じAPI方式を繰り返さない。
- ローカルcloneは予備経路として保持するが、通常ルートの作業では使わない。

## 直チャット保存

2026-09-15の保存は旧連番方式と新方式の混在を許容し、**直近の保存済み記録は023**。実体は `直チャット/2026-09-15_直チャット即時保存_023.md`。

通常の会話は保存しない。たかが明示的に保存を指示した場合だけ保存する。対象を実際に確認できない場合は推測・再構成しない。

新規保存はタイムスタンプ方式へ移行可能とし、旧連番ファイルは変更せず歴史資料として保持する。保存後は、BUD・この引き継ぎ等の現在資料にある最新保存情報を実体と一致させ、読み戻して確認する。

## EXP / REC

- `EXP-0000000060 / REC-0000000010`：VAIO P上の直チャット検索とGitHub APIレート制限。
- `EXP-0000000070 / REC-0000000011`：再開時の状態同期不全・既知事項再調査による作業停滞の運用点検。
- 今回の大手術方針自体は新しい運用判断として確定した。必要なEXP/REC化は一括改修の検証時に重複を確認して判断する。

## 今回までに確定・変更した事項

- 正本リポジトリを `takalenny-blip/ai-project` に確定した。
- `docs/正本リポジトリ.md` を新設し、正本識別情報を固定した。
- `BUD.md` に正本リポジトリを明記した。
- 直チャット020・021・023を保存した。
- 直チャット023は、Claude提案をできるだけ採用すること、旧連番と新タイムスタンプ方式の混在を許容すること、大手術を行うことを記録した。
- `BUD.md` の直チャット最新保存情報を023へ同期した。
- `BUD.md` に大手術の現状・採用改善・改修順序を反映した。
- この引き継ぎの直チャット最新保存情報を023へ同期した。

## 完了した事項

- VAIO Pのシステム軽量化は完了済み。追加軽量化を行わない。
- Chromium 152.0.7977.75の `--disable-gpu` 起動を確認済み。
- Chromiumの日本語入力・スクロール・通常ページ操作を確認済み。
- Git 2.39.5を導入済み。
- `/home/taka/ai-project` のローカルcloneを確認済み。
- 3タイミング運用構造を `rules/運用ルール.md` に反映済み。
- `BUD.md` を現在状態・次の一手のダッシュボードとして整理済み。
- `scripts/search_direct_chat.py` の初回検索成功と403レート制限を確認済み。
- EXP-0000000060 / REC-0000000010、EXP-0000000070 / REC-0000000011を保存済み。
- BUDを「本来の工程」と「トラブル対策」に分離し、トラブル対策が本来の次の一手を乗っ取らない構造へ更新済み。
- 直チャット020・021・023を保存した。

## 未完了・未解決

### 大手術

- タイムスタンプ保存の実装。
- 現在状態の構造化データ化と自動生成経路。
- ローカル検索を第一候補とする検索ツール改修。
- 秘密情報検査の保存前・CI導入。
- 命名・重複・リンク・現在状態整合性のCI導入。
- 機械的ガードの導入。
- EXP/RECの一次記録追跡性改善。
- Issues / Actionsの整理。
- 一括改修後の検証。

### DiMORA

- VAIO P + Chromiumで、認証済みDiMORAページから機械的にデータを取得する具体的方法は未確定。
- 認証状態を安全に扱う方法は未確定。
- 取得データの形式・安定性は未確定。
- 定期実行、変更検知、機種別制御、警告メールまで含めた現行仕様は未確定。

### VAIO P / Firefox ESR

- VAIO P + Firefox ESRで認証済みDiMORAページの `GL_FAVPG_DATA.record[]` 存在確認は未達のまま保留。
- 長いJavaScriptをVAIO P上で手入力する細かなConsole調査は再開しない。

### 家PC

- 家PCは現在のDiMORA作業対象から外す。

## 次回事項

- 通常開始時は `takalenny-blip/ai-project` を正本として、`BUD.md` → `rules/運用ルール.md` の順に確認する。
- 現在は大手術の一括改修を進める。
- 一括改修完了後、検証を行い、整合性が確認できたらDiMORA本来工程へ戻す。
- 「次の一手」は提案であり、明示的な実行指示なしに次工程を勝手に開始しない。ただし、今回のたかの「進めてくれ」は大手術一括改修の実行指示として扱う。

## クローズ判定

- 023保存：**完了**。
- BUDへの023同期：**完了**。
- 現在の引き継ぎへの023同期：**今回完了**。
- 大手術方針：**確定**。
- 大手術一括改修：**進行中**。
- 検証：**未完了**。
- DiMORA技術作業：**大手術完了後に再開**。
- セッション：**作業継続中**。
