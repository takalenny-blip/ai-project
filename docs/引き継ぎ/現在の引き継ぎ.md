# 現在の引き継ぎ

更新日：2026-09-08

## 対象

DIGA / DiMORA 自動化

## 現在の作業地点

Windows PCのChromeで、認証済みDiMORAのお気に入り番組一覧を表示できている。

## 確定

- DiMORAのお気に入り番組一覧のHTML内に `GL_FAVPGM_DATA` が存在する。
- お気に入りデータは `GL_FAVPGM_DATA.record[]` に保持されている。
- 「もっと見る」は追加のサーバー通信ではなく、ページ内の `record[]` を10件ずつ表示するJavaScript処理である。
- 調査の主課題は「もっと見るの通信先を探す」ではない。
- 次の本命は、認証済みDiMORAページを取得し、`GL_FAVPGM_DATA.record[]` を安全に読み出す方法の検証。
- VAIO Pには今回のWindows側調査で変更を加えていない。

## 未確定

- 認証済みDiMORAページをプログラムから取得する具体的方法。
- 認証状態を安全に扱う方法。
- 取得処理をVAIO Pへ移す具体的方法。
- 定期実行、変更検知、DIGA機種別制御、警告メールまで含めた現行仕様。

## 未着手

- Windows側での認証済みページ取得の小規模実証。
- `GL_FAVPGM_DATA.record[]` のプログラムによる抽出実証。
- 抽出結果の保存・比較方法の検討。
- VAIO Pへの導入。

## 失敗・再試行注意

2026-09-08の再開時点で、Chromeでは以下が効かなかった。

- F12
- Ctrl + Shift + I
- Ctrl + U

Chrome右上メニューからのデベロッパーツール操作も、クリックできない状態だった。

したがって、次回これらを前提にした操作をいきなり繰り返さない。

## 次に最初にやる操作

ChromeのDevToolsやCtrl+Uを再度試すことから始めない。

まず既存のDiMORA調査記録を確認し、Windows側で認証済みページのHTMLを取得する別ルートを決める。その後、小さな取得テストを実施する。

## 再開時のバドへの指示

1. このファイルを読む。
2. `docs/00_CURRENT_STATE.md` と `projects/DIGA自動化.md` を読む。
3. 関連REC/直チャットで直前の実験内容を確認する。
4. 上記の「失敗・再試行注意」を尊重する。
5. たかに操作を頼む前に、すでに試した操作と未着手の操作を区別する。
6. 一度に一操作ずつ進め、結果をこの引き継ぎへ反映する。

## 関連資料

- `docs/00_CURRENT_STATE.md`
- `projects/DIGA自動化.md`
- `docs/AI_READ_MAP.md`
- `rules/README.md`
- DiMORA関連REC/直チャット
