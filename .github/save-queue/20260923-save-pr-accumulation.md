# 直前回答保存

- Crow監査の残件としてsave PR蓄積問題を確認。
- 今回の保存経路から #530（save/direct-chat-35844546021）が生成され、openのまま残っている。
- 保存PRは自動生成されるが、save-request-intake.yml は既存save PRがあってもブロックしない設計。
- そのためsave PRが蓄積しても新規保存を止めない状態。
- 現在の正本には、この問題への実装変更はまだ反映されていない。
- 蓄積をどう制御するかの上限値等は勝手に決めず、設計対象として切り出す。
- 次の実装候補：auto-approve-save-pr.yml と保存PRライフサイクルを確認し、自動処理が止まっている箇所を特定する。

直前回答本文：

進めた。Crow監査の残件を確認した。

### save PR蓄積問題
現在、今回の保存経路から **#530（save/direct-chat-35844546021）** が生成され、openのまま残っている。

確認できたこと：
- 保存PRは自動生成されている
- `save-request-intake.yml` は「既存のsave PRがあってもブロックしない」設計
- つまりCrowの指摘どおり、**save PRが蓄積しても新規保存を止めない**状態
- 現在の正本 `docs/現在状態.json` には、まだこの問題への実装変更は反映されていない

ここは勝手に上限値などを決めず、**「蓄積をどう制御するか」自体を次の設計対象として切り出す**のが筋。

次の実装候補は、まず現行の `auto-approve-save-pr.yml` と保存PRのライフサイクルを確認して、どこで自動処理が止まっているかを特定する。