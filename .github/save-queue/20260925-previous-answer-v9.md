## 保存対象種別
会話記録

# 直前の回答

進んでる。さらに原因を正確に切った。

- #652の1回目：**失敗**
  - `保存対象種別` の見出し自体は正しかったが、パーサが次の `##` 見出しまでを値として読むため、`# 直前の回答` まで混ざって **`unknown 保存対象種別: 会話記録 ...`** になっていた。
- これは**保存機構側のキュー形式と実際の入力テンプレートの境界不整合**。
- #652を捨てず、**同じPRを修正**した。
- 修正コミット：`2167207a5c11fa31fe15292929f4822594f445a0`
- 修正後の `save-request-intake` **run #513 が起動済み・queued**。
- `current-state-guard` #854 もqueued。
- まだcanonical save完了とは判定していない。
- なお `main-work-progress-gate` は今回の修正コミットで失敗しているので、ここも**別途原因確認が必要**。

今回は「失敗→新規キュー再投入」はしていない。**同じ #652 を修正して、その実行証拠を追っている。**