# 2026-09-10 直チャット即時保存 0062

[User]
7

[Bud]
確認した。34個のscript要素のうち、外部srcを持たないインラインscriptが7個ある。

ここが重要なので、まずその7個のインラインscriptに `GL_FAVPGM_DATA` が含まれるかだけを個別に調べる。内容そのものは表示しない。

Consoleで次を入力：

`document.querySelectorAll('script:not([src])').length`

出た数字だけ教えて。