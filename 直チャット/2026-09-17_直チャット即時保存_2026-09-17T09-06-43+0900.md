# 直チャット2026-09-17T09-06-43+0900

## ユーザー
直前の回答書き込んで。いま、タイムスタンプの件、ビュー不一致の件、githubの設定変更の件があるが、整理できてる？どういう順番でやる？やりとり書き込んで。

## バド
整理できてる。3件は別々に見えるけど、順番には依存関係がある。

1. **ビュー不一致の原因確認・修正**
   - まずRun #158の「チェックイン済みBUD/引き継ぎと生成結果の不一致」を片付ける。
   - 102保存時に手動で作ったビューが、`generate_current_views.py` の生成契約と一致していなかったのが原因として確認できている。
   - ここは「原因特定 → 修正 → CI成功」まで完了させる。103保存後のRun #159は成功しているが、再発原因を残したままにはしない。

2. **タイムスタンプ方式への完全移行**
   - 次に `save_and_currentize.py` をタイムスタンプ方式へ改修する。
   - 新規保存で連番を採番しないこと、日付またぎでも採番管理が不要になることを機械的に検証する。
   - 旧連番ファイルは履歴として残す。これは「旧方式を新規保存で使う」という意味ではない。
   - ここも実装だけで終わらせず、保存テスト＋CI成功＋GitHub readbackまで確認する。

3. **GitHubの設定変更**
   - その後、mainへの直接pushを抑止するためのGitHub側設定（branch protection等）を行う。
   - ここは現在のGitHub接続から設定変更権限が取れないので、たかがGitHub画面で設定する作業になる。
   - 設定後に、必要なCIチェック・PR経由・squash mergeの運用が実際に成立することを確認する。

つまり、**①ビュー不一致を直す → ②タイムスタンプ方式を実装・検証 → ③GitHub設定を変更・確認**の順。

この3件は全部「保留のまま忘れない」状態で管理していて、タイムスタンプ方式は現在も `pending / monitoring: active`。ビュー不一致はRun #158の原因を特定済みなので、次は修正が必要。GitHub設定変更も未完了。

そして今後は、単に保存し直してCIを通すだけで「解決」とは扱わない。**原因を潰す → 実装 → CI → GitHub readback**までを1セットにする。
