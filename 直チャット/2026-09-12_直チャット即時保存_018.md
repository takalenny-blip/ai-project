[Bud]

確認して、ルールを変更した。

今回の変更は次のとおり。

- `BUD.md` に「返答を書き込んで」を正式な保存合図として追加。
- `rules/運用ルール.md` にも同じルールを追加し、こちらを正本として明記。
- 「返答を書き込んで」→ **直前のBud返答だけ**を保存。
- 直前のUser発言は、この場合の保存対象には含めない。
- 「直前のやりとり書き込んで」→ 今までどおり、直前のUser＋Budの1往復を保存。
- どちらも、個人情報・秘密情報のチェック、添付物の扱い、原文忠実性の確認、GitHub保存後の読み戻し確認を行う。
- 保存対象を実際に取得できない場合は、推測・再構成せず保存未完了とする。

今回のルール変更について、`BUD.md` と `rules/運用ルール.md` はGitHubへ保存し、両方とも読み戻して変更内容を確認済み。

また、今回の「返答を書き込んで」自体も、この新しいルールに従い、**このBud返答だけ**を直チャットへ保存する。