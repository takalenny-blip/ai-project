[User]
直前の1往復を、そのまま保存。番号は空いているものを使う

[Bud]
そう。**ConnManは削除済み**だから、次はサービス停止・無効化を1つずつ進める段階。

前に確認した状態では、**ModemManagerも停止・削除済み**だったので、そこは触らなくていい。

次は、VAIO Pの用途に対して不要なサービスを確認して、**影響の小さいものから1つずつ停止→確認**で進めよう。

まず候補を絞るために、以前確認済みの `systemctl` 結果から判断すると、次の候補は **avahi-daemon** あたり。

ただ、勝手に削除までは進めず、まず**停止・無効化だけ**にするのが安全。
