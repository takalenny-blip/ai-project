[User]

やりとり書き込んで。は、その言葉を含むチャットと、その直後のバドの返答を書き出すルールだ。

[Bud]

了解、たか。ルールをその定義に変更する。

今後「やりとり書き込んで。」が入力された場合は、**その保存合図の言葉を含むUser発言と、その直後に生成するBudの返答**を、1つの直チャット記録として保存対象にする。

つまり、これまでの「合図直前の1往復を保存する」方式ではなく、**「合図を含むUser発言＋直後のBud返答」方式**にする。

今回のこの発言と、この返答もそのルールに従って保存する。
