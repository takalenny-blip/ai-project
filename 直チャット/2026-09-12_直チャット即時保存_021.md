[User]
直前の1往復を、そのまま保存。番号は空いているものを使う

[Bud]
了解。**今後の保存指示はそれで統一する。**

> **「直前の1往復を、そのまま保存。番号は空いているものを使う」**

これなら、番号を埋めることより**保存の正確さを優先**できる。

そして保存時は、俺が勝手に別のやりとりを足したり、要約したりせず、**直前のUser＋Budの1往復そのもの**を対象にする。
