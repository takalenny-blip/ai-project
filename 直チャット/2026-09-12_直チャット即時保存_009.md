[User]
それも終わって、connmanをdisabledしたよ

[Bud]
了解、たか。そこまで進んだな。

今日の実績として、`connman-wait-online.service` の無効化後確認も終わり、さらに `connman` 自体も **disabled** にした、と現在状態を更新する。

ここで一度、ネットワークが実際に生きていることと、NetworkManager側が管理主体になっていることを確認してから、次のサービス整理へ進もう。

VAIO Pで、まずこれを実行して結果をそのまま貼ってくれ。

```bash
systemctl is-active NetworkManager
systemctl is-enabled NetworkManager
nmcli device status
```

まだ他のサービスをまとめて止めたり、Chromium系ブラウザのテストには進まない。