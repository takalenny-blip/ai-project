# 2026-09-10 直チャット即時保存 0057

[User]
https://www.dimora.jp/dc/pc/dimora.do

[Bud]
URL確認。正規のDiMORA PCページ `www.dimora.jp/dc/pc/dimora.do` にいる。

次は、`GL_FAVPGM_DATA` が別名・別形式で埋め込まれていないかを見るため、Consoleで次の一行だけ実行：

`Object.keys(window).filter(k => k.includes('FAVPGM'))`

結果に何か出たら、その**名前だけ**教えて。何もなければ `なし` でOK。
