[User]
さっきのofonoはactive。結果は空白

[Bud]
了解。そこは訂正する。

`ofono` は **active**。`connmanctl services` は**空白**だった、ということだな。

なので、今の段階では「ConnManがWi-Fiを実際に管理している」と断定しない。

次は、現在のWi-Fi経路を直接確認する。

VAIO Pでこれだけ実行して。

```bash
ip route
```

結果をそのまま貼って。

まだサービスの停止・削除も、ブラウザのテストもやらない。