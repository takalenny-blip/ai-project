[User]
直線の返答書き込んで。
では、また必要なルール等確認と修正よろしく。
やりとり書き込んで。

[Bud]
了解、たか。

確認した。現行の `rules/運用ルール.md` には、`やりとり書き込んで` 後のEXP/REC判定、インタビュー、保存・読み戻し、終了時チェックまでは入っている。

今回足すべきなのは、継続作業の状態を明示的に残すチェックで、次を必須項目として扱う。

- 確認済み
- 削除済み／変更済み
- 保留
- 失敗・未完了・未確認
- 現在の次の一手

また、次の一手は過去ログの予定をそのまま採用せず、今回までの確認済み事実・削除済み・保留・未完了から現在化して決める。

今回の `008` は、直前のBud返答だけを保存した。現在のこの `009` は、このUser発言＋このBud返答を保存対象とする。

なお、既存の canonical な `rules/運用ルール.md` を全文取得できない状態で部分更新すると、既存ルールを壊す危険があるため、今回は本文を推測で書き換えず、必要な修正内容を確定した。ルール本体の安全な更新は、全文を保持できる状態で行う。
