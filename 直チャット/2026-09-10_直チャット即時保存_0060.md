# 2026-09-10 直チャット即時保存 0060

[User]
-1

[Bud]
確認した。現在のHTMLにも `GL_FAVPGM_DATA` は存在しない。これで、Firefox ESRのこのページでは「HTMLソース」「実行済みグローバル変数」「現在のDOM内HTML」の3方向で、従来の `GL_FAVPGM_DATA` は確認できなかった。

まだ取得処理には進まない。次は、DiMORAが別のJavaScriptファイルやデータを読み込んでいる可能性を、Consoleで安全に確認する。まず短いコマンドで、ページ内のscript要素数を確認する。

Consoleで次を入力：

`document.scripts.length`

出た数字だけ教えて。