# 2026-09-14_直チャット即時保存_018

## User
やれる？やれるんなら進めて。不要ファイルなどゴミはないほうがいい。おれは削除もいとわないと考えてる。やりとり書き込んで

## Bud
たか、やれる。実際に進めた。

俺の判断は、**BUD.mdの圧縮は今やるべきで、不要ファイルも役割を確認できたものから削除してよい**。ただし「古そう」という理由だけでは消さず、正本との重複・矛盾・一時ファイルかどうかで判定した。

### 実施したこと

1. `BUD.md` を、現在の目的・現在状態・現在の次の一手を中心とする最上位ダッシュボードへ圧縮した。
2. `BUD.md` と `rules/運用ルール.md` を現在の正本の中心として明確化した。
3. `rules/README.md` と `docs/AI_READ_MAP.md` は、すでに最小の入口・読解案内になっているため残した。
4. `docs/全体進行設計図.md` と `docs/引き継ぎ/現在の引き継ぎ.md` は役割があるので残した。
5. `rules/_revert_temp` ～ `rules/_revert_temp9` の空の一時ファイル9個を削除した。これは明確なゴミなので削除してよいと判断した。
6. `rules/直チャット保存合図_厳格定義.md` を削除した。現在の `rules/運用ルール.md` と保存対象の定義が食い違っており、残すとAIを迷わせるため。
7. `rules/運用フロー_3タイミング改定対応表.md` を削除した。3タイミングの内容はすでに `rules/運用ルール.md` に統合済みで、対応表としての役割を終えているため。

### 俺の判断

JUNの「BUDをダッシュボード化する」は採用した。

一方で、**「古いファイルをまとめて削除する」は採用していない**。履歴・一次記録・全体構造まで消すと、将来また同じ迷子状態を作るから。

今回の削除基準は、

- 明らかな一時ファイル
- 正本と矛盾する旧ルール
- 正本へ統合済みで役割を失った対応表

に限定した。

これなら「ゴミは減らす」と「必要な履歴は残す」を両立できる。

### 検証

`BUD.md` はGitHubから読み戻し、圧縮後の内容とSHAを確認した。新しいBUDのcontent SHAは `be5309d664654d850138b6f3eb3c76bdf4c87777`。

また、`_revert_temp` を検索し、現在のGitHub上に残っていないことを確認した。

今回の直チャット保存連番は `018`。

### 現在の状態

- BUD：整理完了
- 明確なゴミ：削除完了
- 正本：`BUD.md` + `rules/運用ルール.md`
- 履歴・一次記録：役割のあるものは維持
- 次の技術作業：VAIO P + Chromiumで認証済みDiMORAから機械可読データを取得する再現可能な経路の検証
