[User]
**確認できた事実**

* 添付ファイルそのものをリポジトリへバイナリ保存するのではなく、**「何を読み込み、何が読み取れたか」をテキストとして一次記録に残す**という方針が合意された。
* 記録の品質を担保するため、情報を以下の項目に厳格に分離する方式が提案された。
* **表示文字**＝画像・文書から実際に読み取った文字
* **画像内容**＝目視できる事実
* **読み取り本文**＝文書から取得したテキスト
* **バドの解釈**＝推測・判断
* **不明／判読不能**＝読み取れなかったもの


* この方式により、「事実」と「バドの解釈」の混同を防ぎ、リポジトリの「画像は主記録ではなく補助記録」という前提とも綺麗に整合する。


* 本ターンのやり取りも含め、最新の直チャットは順次GitHubへ保存・確認されるフローになっている。

**現在決まっていること**

* 今後の直チャット保存プロトコルに、この「添付ファイルのテキスト化・分離記録方式」を正式なルールとして組み込むこと。
* 写真やドキュメントが提示された際は、ファイル名・読み込み宣言・内容のテキスト化・解釈の分離を行ってから直チャットに記録すること。

**JUNの分析・提案**

* **設計・運用上の評価**:
画像やファイルを「そのままバイナリで突っ込む」のではなく、**「AIが何を認識し、どう解釈したか」のテキストデータ（客観的事実と解釈の分離）に昇華して一次記録化する**アプローチは、リポジトリの軽量性を保ちつつ、後からの検証可能性（監査証跡）を最大化する非常に優れた設計です。
* **将来起こりそうな問題**:
画像からテキストを抽出する際に、AI側が誤読（ハルシネーション）を起こして「読めない文字を勝手に補完して記録してしまう」リスクがあります。そのため、読めない部分は必ず「不明／判読不能」として切り離すというルール（今回の分離項目）の厳守が不可欠です。
* **JUNの提案（目的と完了条件）**:
* **目的**: 添付ファイルを含むセッションでも「事実と解釈の分離」が徹底された、高精度な一次記録を残せるようにすること。
* **完了条件**: `rules/直チャット保存プロトコル.md`（または関連ルール文書）へ、今回の「添付ファイル読み取り時の記録フォーマット（表示文字・画像内容・読み取り本文・解釈・不明部分の分離）」を追記・確定させること。
* **変更対象となるファイルや範囲**: `rules/直チャット保存プロトコル.md`

[Bud]
了解。今回は、この内容を正式にルールへ組み込む作業として進めた。

**目的**
添付ファイルを含む直チャットでも、読み取った事実とバドの解釈を混同せず、検証可能な一次記録を残せるようにする。

**完了条件**
`rules/直チャット保存プロトコル.md` に、添付ファイルのテキスト化、`表示文字`、`画像内容`、`読み取り本文`、`バドの解釈`、`不明／判読不能` の分離、読み取り不能時の扱いを正式に追加し、GitHubへ保存後に読み戻して確認すること。

**変更対象**
`rules/直チャット保存プロトコル.md`

実施した変更は完了している。添付については、通常はバイナリそのものを保存せず、読み取れた内容をテキストとして一次記録へ残す方式を正式ルール化した。特に、読み取れない文字や内容を推測で補完しないこと、事実とバドの解釈を明確に分離することを明記した。

GitHubへ保存した後、同じ `rules/直チャット保存プロトコル.md` を読み戻し、追加内容が実際に入っていることを確認した。保存後確認も通過している。

また、このターン自体も直チャット保存プロトコルに従い、`0024` としてこのファイルへ保存する。保存後に読み戻して、原文忠実性まで確認する。

したがって今回の完了条件は、**「添付ファイルのテキスト化・事実と解釈の分離方式を、直チャット保存プロトコルへ正式組み込み済み」**となる。
