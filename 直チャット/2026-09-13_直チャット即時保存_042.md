[User]
fjagsの前に.configが間にはいる。でフォルダは存在
やりとり書き込んで

[Bud]
了解。`chromium-flags` は `~/.config` の下にあり、フォルダ自体は存在していると確認した。
さっき俺が `~/chromium-flags` と決め打ちしたのはミス。以後、そのパスを前提にする。
今回の内容は、現在のChromium作業に必要な環境情報の確認だが、一時的な作業情報なのでEXP/REC化はしない。
次の一手は、実際のパスを確認したうえで一つだけ指示する。
