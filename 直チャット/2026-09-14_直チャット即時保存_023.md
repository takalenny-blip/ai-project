# 直チャット即時保存 023

## たか
ソノコマンドだよ！Chromiumだからじゃない？実際はchromium。大文字小文字　　JUNから　　* **確認できた事実** 
* 9/13の直チャットファイル（`048`など）を直接確認した結果、VAIO PでのChromium起動コマンドとして `~/.config/chromium-flags/launch-light.sh` が存在することが判明した。 
 
 
* GitHub上に一次記録自体はしっかり存在しているにもかかわらず、特定のキーワード（`Chromium` など）での検索がヒットしなかったことから、問題の本質は「一次記録の欠如」ではなく「検索経路・到達性の不足」であることが実測によって裏付けられた。 
* 「やりとり書き込んで」の指示に基づき、この内容を含む `直チャット/2026-09-14_直チャット即時保存_023.md` の保存および読み戻し確認を進めるフェーズにある。 
 
 
* **現在決まっていること**
* 索引を先回りして作ってごまかすアプローチは廃止する。 
 
 
* ボトルネックとなっている「GitHub上の一次記録をAIがキーワードから直接・迅速に発見できない検索経路の弱点」を解決するため、GitHub検索の挙動、APIによる取得・全文検索、あるいはPythonによるローカル全文検索の検証へ進むこと。 
 
 
* 今回の実測結果をふまえ、この検索構造の問題をEXP/RECの対象として正式に扱い、次の実験フェーズへと移行すること。 
 
 

* **JUNの分析・提案**
* **目的**: 一次記録が存在するのにAIがそこへ辿り着けない「検索経路の盲点」を技術的に解消し、リポジトリ全体の知識再利用性を根本から高めること。 
* **対象**: AIの検索メカニズムと、リポジトリ内ファイルの全文検索・取得経路（GitHub検索の特性、Python等によるローカル検索の可能性）。 
* **変更内容**: 単なる手動検索や不確実なキーワード指定に頼らず、リポジトリ全体を正確かつ網羅的にスキャンできる軽量な検索・到達テストの実施。 
* **完了条件**: 「なぜ検索でヒットしなかったのか」の理由が技術的に解明され、次回の情報探索を確実に成功させられる代替手段または検証結果が1件確立されること。　　やりとり書き込んで

## バド
うん、そこは重要。今回の検索失敗について、`Chromium` と実際の文字列 `chromium` の大文字小文字差は、原因候補としてまず検証するべき。現時点では「大文字小文字が原因」と断定はしない。GitHub検索側の検索仕様・コネクタ側の検索/index挙動も候補に残す。

**JUNの提案：採用。** ただし、今は索引を作らない。まず検索経路そのものを実測する。

確認済みの事実は、`048` に実際の起動対象 `~/.config/chromium-flags/launch-light.sh` があり、一次記録を直接読めば発見できること。したがって一次記録の欠落ではない。

**次の一手は1回の比較テストに絞る。** `Chromium` / `chromium` / `launch-light.sh` / `~/.config/chromium-flags/launch-light.sh` を同じ検索経路で比較し、大文字小文字差でヒットが変わるのか、それとも検索/index側の問題なのかを切り分ける。その結果をEXP/REC化し、再現可能な検索方法を1本確立する。

ループはしない。今回は「046〜058を直接確認して事実を特定する」段階を完了し、次は「検索経路の比較実験」に進む。