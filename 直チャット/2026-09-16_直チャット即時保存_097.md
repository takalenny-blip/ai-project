[Bud]
実装した。今回は**実際にGitHubへコード変更を入れた**。

### 実装内容

- `scripts/check_atomic_save_commit.py` を新規追加
- GitHub Actions の先頭に **Atomic Save Commit 強制チェック**を追加
- `直チャット/` または
  - `docs/現在状態.json`
  - `BUD.md`
  - `docs/引き継ぎ/現在の引き継ぎ.md`
  
  のどれかを変更したコミットは、**4点セット＋直チャット1件を同一コミットに含むことを必須化**。
- 条件を満たさない分割コミットはCIで明確に拒否する。
- 既存の整合性チェックはそのまま維持。

そして、これらを**1コミット**で `main` に入れた。

コミット：
`228431e50d27a35f6e1023f550bd16b3a6a9dc09`

つまり、095/096で起きた  
**「直チャットだけ保存 → BUDだけ更新 → 現在状態更新」**  
という経路を、CI側でも機械的に止めるところまで実装した。

まだCIの実行結果は返ってきていないので、**実装完了・CI検証待ち**の状態。 
