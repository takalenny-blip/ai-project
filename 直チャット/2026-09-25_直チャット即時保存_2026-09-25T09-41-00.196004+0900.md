# 直チャット2026-09-25T09-41-00.196004+0900

# クロウ監査結果と今回のやりとり保存依頼

## 保存対象種別

会話記録

## 保存対象

たかから提示されたクロウの親タスク／子タスク状態管理監査結果、およびそれを受けた今回の保存指示。

## クロウ監査結果

読んだ版：main `0912c20`（#636、2026-09-25 09:31 JST）。新規clone、GitHub Actions API（check-runs／jobs）、PR #634・#635のメタデータで確認。リポジトリへの変更はしていない。

### 最重大な事実

#634はintakeが失敗したのではなく、intakeを一切通っていなかった。\
`chat-save-request/20260925-parent-child-control-fix` のキューPR自体が直接mainへマージされ、コミット `0f45c38` として反映された。その結果、キューの生ファイル `.github/save-queue/20260925-parent-child-control-fix.md` が未処理のままmainに残った。

同種の放置ファイルが他に3件あり、合計4件の未処理キューファイルがmainに残っていることを確認。

キューPRを直接マージでき、GitHub上では `merged=true` に見えてしまう一方、intake（子タスク）を経由しない経路が現に存在していた。これは自動再投入バグではなく、キューPRを直接マージできる構造的な抜け穴。

### 親子状態管理

`scripts/save_lifecycle_status.py` は `queue_pr`、`final_save_pr`、`canonical_readback` から6状態を導出するが、実パイプラインから呼ばれておらず、テストからしか使われていない。

そのため、正しい状態判定ロジックは存在するものの、実際の判断に接続されていない。

親タスク側の状態（未実行／投入済み／処理中／完了）を表すフィールドも `docs/現在状態.json` に存在しない。

### 再試行・再投入

コード上の自動再投入機構は存在しない。`retry` は `auto-approve-save-pr.yml` 内のmerge API呼び出しの短時間リトライのみ。`requeue`／`re-queue` のコードは確認されなかった。

#634→#635は自動再投入ではなく、人（バド／たか）の手動判断による再投入だった。ただし、その前提となった「#634のintakeが失敗した」という認識自体が事実と異なり、#634はintake未実行の直接マージだった。

#635もマージされずクローズされ、最終的な正常保存は#636の正規4ファイル一括更新で成立した。

### ループ・重複

自動的な無限再投入は現状コードに存在しない。一方、重複子タスクを防ぐ検査、retry回数、retry理由、親子対応付けを記録する仕組みは存在しない。

したがって自動ループはないが、人が同じ誤認を繰り返せば手動で同様のループを発生させられる。

### concurrency

`save-intake-global` のconcurrencyはintakeの同時実行を制御するだけで、投入、重複投入、キューPRの直接マージを防がない。ループ対策の根拠にはできない。

### 回帰テスト

既存テストは子のopen/closedと完了関係の一部を保証しているが、blocked、重複子タスク、retry回数・理由・親子追跡などは未整備。

### 未解決問題

1. `chat-save-request/*` のキューPRを人が直接mainへマージできる。
2. `save_lifecycle_status.py` が実パイプラインに未接続。
3. 親タスク状態フィールドがない。
4. 重複子タスク検査、retry回数・理由、親子対応付けがない。
5. mainに未処理キューファイルが4件残っている。
6. 過去の同種「キューPR直接マージ」が今回以外にも起きていないか、全履歴は未走査。
7. 4件の未処理キューファイルが別経路で保存済みか、重複になっていないか未確認。

## 今回のやりとりで確定した認識

今回の目的は、単に#635を通すことではない。

「同じ失敗を二度と起こさない」ことを最優先に、親タスク／子タスクの状態誤認、未完了の失敗扱い、重複再投入、再試行ループ、queue滞留、そしてキューPRの直接マージによる未処理状態をまとめて潰す必要がある。

また、はまった場合は勝手に次の子タスクを積まず、クロウに確認する。確認依頼はチャット上に実体の文章を出し、やり方を勝手に変更しない。

## ユーザー依頼

「やりとり書き込んで」

この依頼を新しい保存親タスクとして扱い、会話記録として保存キューへ委譲する。
