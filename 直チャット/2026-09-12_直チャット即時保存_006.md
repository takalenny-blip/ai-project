[User]
taka@vaiop:~$ sudo systemctl disable --now connman-wait-online.service
[sudo] taka のパスワード:
Removed "/etc/systemd/system/network-online.target.wants/connman-wait-online.service".
taka@vaiop:~$

[Bud]
了解。`connman-wait-online.service` の無効化は実行できた。

これは今回の軽量化候補の中でも、起動時に2分9秒かかっていたものなので、効果が期待できる。

ただし、ここで次のサービスをいきなり止めない。まず変更後の状態を確認する。

VAIO Pでこれだけ実行して。

```bash
systemctl is-active connman-wait-online.service
systemctl is-enabled connman-wait-online.service
```

結果を貼って。