# 直チャット即時保存 033

## たか
エラーメールが３件来たぞ　　やりとり書き込んで。

## バド
了解。まず今回のエラー発生を033として記録する。ここで慌てて修正を重ねず、3件がそれぞれ別のpushに対するActions失敗なのか、同じ原因の連続失敗なのかを切り分ける。

現在までに確認できていること：
- 以前のrun #5/#6はsuccess。
- 今回追加したのは、生成結果とチェックイン済みBUD/引き継ぎを `cmp` で完全一致させるCI検証。
- そのため今回の3件は、移行作業の途中で複数コミットが連続してActionsを発火した結果である可能性がある。
- ただし、これはまだ推測であり、エラーログを確認するまでは原因確定とはしない。

生成器 `scripts/generate_current_views.py` を読み戻したところ、BUD/引き継ぎは `docs/現在状態.json` の内容から機械生成される構造になっている。したがって、手作業でビューを書き換える方式を続けるより、生成器の出力そのものを正本ビューとして採用する必要がある。

今回の判断：3件のエラーを見て、同じ箇所を手作業で何度も直すループには入らない。まず失敗runのjob/logを直接確認し、`cmp` の不一致なのか、別のエラーなのかを確定する。その結果に応じて最小修正する。

たかにGitHub画面を操作してもらう必要はない。バド側でActionsの実行とログを確認する。
