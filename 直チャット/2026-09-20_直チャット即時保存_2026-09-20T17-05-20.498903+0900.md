# 直チャット保存要求（Claudeレビュー反映・全部対応）

## User
全部対応して。できる？ループすんなよ。順序立てて確実に。やり取り書き込んで。

## Bud
できる。今回はレビューを受けて議論だけで終わらせず、順序を固定して実装・検証・merge・readbackまで進める。

1. **外部成果物preflight gateを実装**
   - `next_step.prerequisites` を必須化。
   - 前提状態を `verified / unverified / missing / invalid` に分離。
   - 工程状態を `readiness=ready / blocked` として別軸化。
   - readyは全前提verifiedの場合だけ許可。
   - blockedには `blocked_reason` と `unblock_action` を必須化。
   - verifiedには確認方法・確認日時の証拠を必須化。
   - `current_state_guard.py` と `resume_check.py` にゲートを反映。
   - ready/unverified、blocked/unverified、verified without evidenceのテストを追加。
   - rules/運用ルール.mdへpreflight gateを追加。

2. **現在状態を是正**
   - DiMORA実データ検証のnext_stepを `readiness=blocked` / `status=blocked` に変更。
   - 実機エクスポートJSONが確認できるまでは実データ検証を実行済み扱いしない。
   - JSON本体は公開リポジトリへ追加しない。

3. **証拠状態の瑕疵を是正**
   - `verification_required[]` の過去の `done` を、独立証拠が紐付いていないものは `recorded_unverified` と明示。
   - `current_position` にPR/merge SHAによる根拠を追加。
   - 自動承認はゲートであって独立検証証拠ではないことをルール化。
   - retired済みVAIO Pの現役制約記述を除去。
   - `check_repo_integrity.py` を新しい証拠状態契約へ追従。

4. **検証**
   - PR #160：preflight gate実装。CI成功、taka2-dev承認、merge確認。
   - PR #160 merge SHA：`32c6e1de948eebc815a371c3e28ec08ad2baf5c0`
   - PR #161：証拠状態契約を実装。current-state-guard成功、Current state views成功、taka2-dev承認、merge確認。
   - PR #161 merge SHA：`a02a387f238def697f14ce26768b87207bb66e58`
   - main readbackでnext_stepがblockedであること、guard/resume_check変更がmainへ反映されたことを確認。

5. **未完了**
   - 実機エクスポートJSONそのものは現在利用可能であることを確認できていない。
   - よってDiMORA実データ正規化はまだ実行していない。
   - 実データ取得・preflight確認後にのみreadyへ進める。

今回の目的は、同じ「進んだふり」のループを止めること。実体が確認できないものをready/完了と扱わない契約までmainへ反映した。
