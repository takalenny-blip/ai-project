# 直チャット即時保存 032

## たか
ループしてないよね？いいなら進んで。やりとり書き込んで

## バド
ループしていない。前段で失敗原因の方向を確認し、今回は次の実装単位へ実際に進めた。

実施したこと：
1. `docs/現在状態.json`を再確認し、031を現在の保存正本として確認。
2. 既存 `BUD.md` を、現在状態JSONから生成されるビューの内容へ切り替えた。
3. 既存 `docs/引き継ぎ/現在の引き継ぎ.md` も、現在状態JSONから生成されるビューの内容へ切り替えた。
4. GitHub Actionsに「生成結果とチェックイン済みBUD/引き継ぎが完全一致すること」の機械検証を追加した。

これは「現在状態JSON → BUD/引き継ぎ」という一方向フローへ移すための、予定していた次の小単位そのもの。生成器を再設計したり、同じ失敗原因を再調査したりはしていない。

今回の採用判断：この移行を進める。理由は、現在の生成器が決定的に候補を作れており、run #5/#6で決定性検証が成功しているため。ここで既存ビューを生成結果へ切り替え、CIで一致を機械的に固定するのが自然な次段階。

注意点：この時点では、今回追加した「チェックイン済みビューとの一致」まで含むActionsの最終結果は未確認。したがって、まだ「移行完了」とは扱わず、CI検証待ちとしている。

次の一手：今回のActionsが成功することを確認する。成功なら `docs/現在状態.json` の移行状態を完了側へ現在化し、BUD/引き継ぎをその状態から再生成して再検証する。失敗した場合だけ、その失敗箇所を調べて最小修正する。
