[User]
ログインした

[Bud]
よし。ここから目的の最小確認に入る。

Firefox ESRで、いま表示している認証済みDiMORAページのまま、まず `Ctrl+U` を押してページのソース表示を開く。

次に `Ctrl+F` で `GL_FAVPGM_DATA` を検索する。

- 見つかった → その文字列が見えるかだけ確認して、「見つかった」と送る。
- 見つからない → 「見つからない」と送る。

ソースの内容をコピーして貼る必要はない。ログイン情報なども送らなくていい。
