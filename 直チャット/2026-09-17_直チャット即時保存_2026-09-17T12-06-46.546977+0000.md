# 直チャット保存要求

## User
やり取り書き込んで

## Bud
了解。今の保存入口を使って、このやり取りを正規保存ルートへ渡す。

この保存要求は `.github/save-queue/` を中継に使い、canonical な `直チャット/`、`docs/現在状態.json`、`BUD.md`、`docs/引き継ぎ/現在の引き継ぎ.md` は GitHub Actions 上の `scripts/save_and_currentize.py` だけが生成・更新する。
