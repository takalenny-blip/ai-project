[User]
見つからない

[Bud]
確認した。VAIO PのFirefox ESRで、認証済みDiMORAページのページソースを検索しても `GL_FAVPGM_DATA` は見つからなかった。

これは「Firefox ESRでは存在しない」と断定する結果ではなく、まず現在のページソースにその文字列が出ていない、という事実として扱う。

次は、別の取得方法に飛ばず、Firefox ESR上でページが実際に読み込んだJavaScript側を最小確認する。設定変更や認証情報の取得はしない。

まずページソース画面を閉じて、元のDiMORAページに戻って待機していて。次の指示を出す。
