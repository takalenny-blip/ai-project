# 2026-09-10 直チャット即時保存 0059

[User]
www.dimora.jp

[Bud]
ホスト名は `www.dimora.jp` で確認できた。次は、ページ内に `GL_FAVPGM_DATA` が実際に読み込まれているかを、より単純な JavaScript で確認する。取得・表示はまだ行わない。

Firefox の Console で次を入力して Enter：

`document.documentElement.innerHTML.indexOf("GL_FAVPGM_DATA")`

出た数字だけ教えて。`-1` なら現在のHTMLにはその文字列がない。0以上なら存在位置が確認できる。