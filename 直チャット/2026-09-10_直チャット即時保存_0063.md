# 2026-09-10 直チャット即時保存 0063

[User]
27

[Bud]
確認した。`document.querySelectorAll('script:not([src])').length` の結果は **27**。

これで、現在のDiMORAページには「外部srcなしのインラインscriptが27個ある」ことを確認した。

次は、その27個のインラインscriptの中だけを対象に、`GL_FAVPGM_DATA` という文字列が存在するかを安全に確認する。データ本文や認証情報は表示しない。

Firefox ESRのConsoleで、次の1行をそのまま実行して、結果が `true` か `false` かだけ教えて。

`Array.prototype.some.call(document.querySelectorAll('script:not([src])'), function(s){return s.textContent.indexOf("GL_FAVPGM_DATA")>=0;})`
