# 2026-09-10 直チャット即時保存 0056

[User]
undefined

[Bud]
確認した。VAIO P の Firefox ESR では、現在の認証済み DiMORA ページ上で `typeof GL_FAVPGM_DATA` は `undefined`。

これは「ページソースにもなく、現在のグローバル変数にもない」という事実。Windows Chrome で確認できた状態とは異なるので、ここで取得処理へ進まない。

次は原因を切り分けるため、Console で **`location.href`** だけ実行して、表示されたURLのうちドメインとパスだけ教えて。クエリ文字列や個人情報があれば貼らなくていい。
