[User]
taka@vaiop:~$ ip route
default via [ゲートウェイ] dev wlp2s0 proto dhcp src [ローカルIP] metric 600
169.254.0.0/16 dev wlp2s0 scope link metric 1000
[ローカルネットワーク] dev wlp2s0 proto kernel scope link src [ローカルIP] metric 600
taka@vaiop:~$

[Bud]
これは `ip route` の結果だな。Wi-Fiインターフェース `wlp2s0` にデフォルト経路が入っていることは確認できる。

ただし、これだけでは `connman` と `NetworkManager` のどちらが実際にWi-Fiを管理しているかは確定できない。

なので、まだ停止・削除はしない。

次は、さっきお願いしたこれだけを実行して。

```bash
systemctl is-enabled connman connman-wait-online NetworkManager NetworkManager-wait-online ModemManager ofono dundee
```

結果をそのまま貼って。そこから一つずつ整理する。

ブラウザのテストにはまだ進まない。