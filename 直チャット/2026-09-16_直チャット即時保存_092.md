# 直チャット092

## User

claudeにきいてみた。　　実際に該当コミット・スクリプトを確認しました。**「なぜ今回も複数コミットに分かれたか」について、推測ではなく特定できる原因が見つかりました。**

# 確認した事実

- HEADが`39374162...`であることを確認しました。090サイクルの5コミット（`直チャット090保存`→`現在状態を090と採用済み改善へ更新`→`090の保存実体SHAを現在状態へ同期`→`BUDを090と採用済み改善へ同期`→`引き継ぎを090と採用済み改善へ同期`）は、**すべて`save-090-adopt-audit`という作業ブランチ上のコミット**で、PR #13としてmainへマージされていました。
- `.github/workflows/current-state-views.yml`の`on.push`には**`branches`指定がありません**。つまりこの作業ブランチへのpushでも、mainへのpushと同様にworkflowが毎回発火します。これが#127〜#130がPR未マージの段階で失敗していた直接の経路です。

# 根本原因（新しく特定できたこと）

**`save_and_currentize.py`は、`docs/現在状態.json`の`direct_chat`ブロックしか更新できません。**

090サイクルは、単なる直チャット保存ではなく「採用済み改善をJSONへ反映する」作業を伴っていました。これは`save_and_currentize.py`の対応範囲外なので、手動で`現在状態.json`を編集し、`generate_current_views.py`を個別実行し、BUD/引き継ぎを別コミットで同期する旧来の5ステップに戻らざるを得なかった、という診断です。

# Claudeの提案

1. `save_and_currentize.py`を汎用化し、現在状態JSONへの変更を一つのパッチとして受け取り、ビュー再生成・整合性チェック・1コミットまで一括化する。
2. workflowの`push` triggerを`branches: [main]`に限定し、作業ブランチの中間コミットではCIを発火させない。
3. 優先順位は、低リスクで即効性のある2を先に、その後1を設計する。

## Bud

Claudeの調査結果を受け取った。これは推測ではなく、該当コミット・スクリプト・workflow設定から原因を特定したものとして記録する。

現時点では提案の採用・実装はまだ行わない。まず、`on.push`のmain限定と`save_and_currentize.py`汎用化を、それぞれの影響範囲・既存CIとの整合・再発防止効果で比較し、設計を決める。

今回の重要な発見は、5件のfailureが単純な操作ミスではなく、**作業ブランチでもworkflowが発火する設定**と、**状態更新スクリプトがdirect_chat限定だったこと**の組み合わせで発生した、ということ。

このやりとりを092として一次記録に保存する。
