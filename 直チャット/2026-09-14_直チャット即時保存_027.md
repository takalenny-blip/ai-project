# 2026-09-14 直チャット即時保存 027

## User
修正すべきところ、即やれ。そして正確に報告しろ。やりとり書き込んで

## Bud
たか、即対応した。今回の原因を踏まえ、まず「どこを直すべきか」を実ファイルで再確認した。

結論：今回の再発防止で必要なのは、3タイミングを増やすことではない。既存の終了処理にある「現在状態を必要な現在資料へ反映する」という規定を、実際に再開事故を止められる形まで具体化して、BUDと現在の引き継ぎへ同期した状態を正本として固定することだった。

確認できた事実：
- `rules/運用ルール.md` には、終了時に必要な現在資料へ反映し、GitHubへ保存した場合は読み戻し確認し、次回の現在状態と次の一手を確定する規定が既にある。したがって「ルールが全く無かった」は誤り。
- 一方、今回の実運用では、その終了処理が実際に実行・検証されないまま、BUD／現在の引き継ぎが古い状態で残った。
- そのため、ルールの理念はあっても、再開時の誤作動を止める実効性が不足していた。

即時修正した箇所：
1. `BUD.md` に「再開点の同期原則」を明記し、直前セッションで確定した次の一手をBUDと現在の引き継ぎの両方へ反映すること、次回起動時にBUD・引き継ぎ・実際の最新一次記録が食い違えば具体操作へ進まず不一致を解消すること、具体操作を推測しないことを明記した。
2. `docs/引き継ぎ/現在の引き継ぎ.md` に同じ再発防止条件と、今回の不整合の原因を明記した。
3. 両ファイルをGitHubへ更新し、直後に読み戻して内容を確認した。
4. `直チャット/2026-09-14_直チャット即時保存_026.md` も保存・読み戻し済みであることを確認した。

検証：
- 修正前の状態を実データで再現すると、古い引き継ぎから「DiMORA検証」が次の一手として出て、さらにバドが `chromium --disable-gpu` を勝手に具体化する事故につながる。
- 修正後は、BUDと現在の引き継ぎの双方に、第一段階が `python3 scripts/search_direct_chat.py chromium`、その完了後にDiMORA検証、という順序が明記されている。
- また、現在資料だけで具体操作が確定しない場合は推測しない条件を明記したため、今回の `chromium --disable-gpu` への勝手な飛びを止められる。

重要な残課題：
`rules/運用ルール.md` 自体にも「BUDと現在の引き継ぎの同期」「同期失敗時はクローズ不可」という、より強い実行条件を明文化する余地がある。ただし同ファイルは今回取得した内容がツール側で途中切断され、全文を安全に再構成できないため、原文を壊して更新することはしなかった。ここを推測で全面置換しない判断をした。

したがって、今回「即やった」修正はBUDと現在の引き継ぎの同期・再開条件の固定まで。運用ルール本体の追加修正は未実施であり、未実施のまま正確に報告する。

現在の状態：
- 直チャット027：このやりとりを保存対象として作成。
- BUD：同期原則を反映済み・読み戻し確認済み。
- 現在の引き継ぎ：同期原則と今回の原因を反映済み・読み戻し確認済み。
- 運用ルール：既存規定は確認済み。追加修正は未実施。
- 次の一手：VAIO Pで `python3 scripts/search_direct_chat.py chromium` の実行テスト。
