# BUD — バドのための最上位ダッシュボード

更新日：2026-09-15

## 1. ここで最初に確認すること

このファイルは、`ai-project` の**現在の目的・現在状態・現在の次の一手**を短時間で把握するための最上位ダッシュボード。

- 作業手順・保存・履歴調査・EXP/REC・外部提案対応等の正本：`rules/運用ルール.md`
- 読解順の補助：`docs/AI_READ_MAP.md`
- `rules/README.md` は入口だけを示す補助資料
- 過去資料の「次にやること」は現在の命令ではない。現在資料と実績で現在化してから判断する。

## 2. 活動の中心

- **たか**：実際に試し、使い、経験し、最終判断する中心。
- **バド**：たかと協働し、調査・整理・設計・分析・提案・実装・検証・経験資産化を担う。
- **JUN / Claude / その他**：必要時に使う補助手段。提案は事実と意見を分け、バド自身の採否判断を明示してから扱う。

## 3. リポジトリの目的

たかの実経験を、成功・失敗・判断・気づきを含む一次記録として蓄積し、AIが過去と現在をつなげて再利用できる状態を作る。将来的には経験ログから記事素材を整理し、Bloggerへの記事作成・投稿へつなげる。

## 4. 現在の最重要テーマ

### VAIO P

- Debian 12 i686 / Intel Atom Z520 / 2GB RAMの軽量環境。
- システム軽量化は完了。追加軽量化へ戻らない。
- Chromium 152.0.7977.75は `--disable-gpu` で起動可能。日本語入力・スクロール・通常ページ操作を確認済み。
- バッテリー不調・発熱があるため、必要時のみ起動し、作業終了後は停止する。
- Git 2.39.5を導入済み。
- `/home/taka/ai-project` のローカルcloneを確認済み。

### DIGA / DiMORA自動化

現在の技術的な目的は、**VAIO P + Chromiumで、認証済みDiMORAページから機械的にデータを取得できる経路を1つ再現可能な形で確立すること**。

確認済みの基準情報：
- Windows + Chromeの認証済みDiMORAでは `GL_FAVPGM_DATA.record[]` を確認済み。
- 「もっと見る」はページ内 `record[]` の追加表示であることを確認済み。
- VAIO P + Firefox ESRでは `GL_FAVPGM_DATA` が見つからなかった。
- Firefox ESRでの長いJavaScript手入力による細かなConsole調査は非効率として打ち切り、同じ方法を繰り返さない。
- 家PCをDiMORA取得方式の現在の検証環境にはしない。

### 一次記録検索経路の現在状態

- `scripts/search_direct_chat.py` はVAIO P上で実行できた。
- `python3 scripts/search_direct_chat.py chromium` の初回実行では直チャット内容が実際に出力された。
- 再実行では `403 rate limit exceeded` が発生した。
- 原因は、GitHub Contents APIで `直チャット/` 一覧取得後に各ファイルを個別取得する実装により、APIリクエスト数が多くなり未認証レート制限に到達したことと判断。
- **同じAPI方式を繰り返さない。** ローカルcloneを利用し、APIを大量に叩かない検索方式へ切り替える。

### 今回の運用上の問題

2026-09-15、たかから、前日に「途中で全く進まない」「すでに調べたことを再び調べさせようとする」問題が起きたとの指摘があった。

確認済みの関連問題：
- 過去にBUD / 引き継ぎ / 直チャットの状態同期が崩れ、古い次の一手へ戻った。
- 資料から確定していない具体操作をバドが補った事例がある。
- VAIO Pの実際のGUI状態を引き継げず、同じ案内を繰り返した事例がある。
- 手入力困難・コピー＆ペースト困難という実機制約を前提にできていない場面があった。
- 既知の失敗条件であるGitHub APIレート制限を避ける判断が必要になった。

この問題を運用構造として点検するため、**JUNへ相談することを採用した**。JUNの回答は事実確認後、バドが採否判断する。

## 5. 現在の次の一手

**第一段階：JUNへ、再開時の状態同期不全・既知事項の再調査・作業停滞の原因と最小限の再発防止策を相談する。**

相談内容は `直チャット/2026-09-15_直チャット即時保存_055.md` に保存済み。

JUN回答の完了条件：
- 事実／原因候補／構造的原因が分離される。
- 既存資料を実際に確認したうえで、問題箇所と防止策が示される。
- 現在のルールで防げる問題／防げない問題が分かれる。
- 必要な変更対象と、変更不要な対象が明示される。
- ルール増加による運用負荷も評価される。
- 不明点はまずJUN側で調査し、それでも必要な点だけたかへ確認する。

**第二段階：JUN回答を受け、バドが採否判断したうえで、必要な資料だけを変更・検証する。**

**第三段階：運用問題が解消したことを確認後、VAIO Pのローカルcloneを使ったAPI非依存の直チャット検索へ戻る。**

第一段階以降を完了するまでは、過去に確認済みの事項を理由なく再調査しない。

## 6. 経験DB・一次記録

- 直チャット・EXP・RECを中心に経験を蓄積する。
- ID管理の正本：`指針等/ID管理台帳.md`
- 経験DBの確定仕様：`docs/2026-09-05_企画書_経験DB運用仕様_確定版.md`
- EXP-0000000030の履歴調査では、REC17件中14件の一次記録が未特定のまま。未追跡を追跡済みとは扱わない。
- `EXP-0000000060 / REC-0000000010`：VAIO P上の直チャット検索とGitHub APIレート制限。
- `EXP-0000000070 / REC-0000000011`：再開時の状態同期不全・既知事項再調査による作業停滞の運用点検。

### 直チャット

- 通常の会話は保存しない。
- **たかが明示的に保存を指示した場合だけ保存する。**
- 保存対象・保存後処理の正本は `rules/運用ルール.md`。
- 対象を実際に確認できない場合は推測・再構成しない。
- 2026-09-15の保存連番055を今回保存済み。

## 7. 構造整理の方針

現在の正本は、次の2本を中心とする。

1. `BUD.md`：現在の目的・状態・次の一手
2. `rules/運用ルール.md`：現在の行動手順・保存・判断ゲート

補助・歴史資料は、役割がある限り残す。**ファイル数だけを理由に削除しない。**一方、同じ現在情報を重複して持つファイルや、役割を失ったファイルは、確認後に削除・統合してよい。

現在、`rules/README.md` と `docs/AI_READ_MAP.md` は入口・読解順に役割を限定済み。`docs/全体進行設計図.md`、`docs/引き継ぎ/現在の引き継ぎ.md`、EXP/REC、直チャット等は、それぞれ全体構造・セッションスナップショット・一次記録／履歴として役割があるため、理由なく削除しない。

### 再開点の同期原則

- 直前セッションで確定した「現在の次の一手」は、会話だけに残さず、終了時に `BUD.md` と `docs/引き継ぎ/現在の引き継ぎ.md` の両方へ反映する。
- 次回起動時、BUD・引き継ぎ・実際の直チャット最新記録の間に現在状態の不一致があれば、具体操作へ進まず不一致を解消する。
- 「次の一手」と「具体的なコマンド／操作」を同一視しない。現在資料から具体操作が確定できない場合、バドは推測して実行しない。
- 2026-09-14の実証では、直チャット025に「検索スクリプトを先に実行し、その後DiMORA検証」という具体的順序が保存されていた一方、旧引き継ぎは006までしか反映されていなかった。この不整合を原因として確定した。
- 2026-09-15の問題を受け、既知の確認済み事項・既知の失敗条件を、理由なく再調査・再実行しないことを現在状態として明示する。

## 8. 安全・判断原則

- 認証情報・セッション情報・Cookie等の秘密情報を保存しない。
- 過去の計画を現在の命令として再実行しない。
- 外部提案は、事実／提案を分離 → バド自身の採否判断 → 理由・懸念 → 次の行動 → 実行 → 検証、の順で扱う。
- 完了時は、確認済み・変更・未完了・保留・次の一手を明確にする。
- 終了時に確定した現在状態・次の一手は、BUDと引き継ぎへ同期してからクローズする。

## 9. 今このファイルを読んだAIが答えられるべきこと

**目的**：経験を蓄積・再利用できるAI協働基盤を作る。

**現在の技術目的**：VAIO P + Chromiumで認証済みDiMORAから機械可読データを取得する再現可能な経路を1つ確立する。

**現在の直近の一手**：まずJUNへ、再開時の状態同期不全・既知事項の再調査・作業停滞について相談し、回答を検証する。その後、必要な運用資料だけを修正し、運用問題の解消を確認してからVAIO Pのローカルcloneによる直チャット検索へ戻る。

**ルールの正本**：`rules/運用ルール.md`。
