# BUD — バドのための最上位ダッシュボード

更新日：2026-09-15

## 1. ここで最初に確認すること

このファイルは、`ai-project` の現在の目的・現在状態・現在の次の一手を短時間で把握するための最上位ダッシュボード。

- 正本リポジトリ：`takalenny-blip/ai-project`
- 正本識別情報：`docs/正本リポジトリ.md`
- 作業手順・保存・履歴調査・EXP/REC・外部提案対応等の正本：`rules/運用ルール.md`
- 読解順の補助：`docs/AI_READ_MAP.md`
- `rules/README.md` は入口だけを示す補助資料
- 過去資料の「次にやること」は現在の命令ではない。現在資料と実績で現在化してから判断する。

## 2. 活動の中心

- たか：実際に試し、使い、経験し、最終判断する中心。
- バド：調査・整理・設計・分析・提案・実装・検証・経験資産化を担う。
- JUN / Claude / その他：必要時に使う補助手段。提案は事実と意見を分け、バド自身の採否判断を明示する。

## 3. リポジトリの目的

たかの実経験を成功・失敗・判断・気づきを含む一次記録として蓄積し、AIが過去と現在をつなげて再利用できる状態を作る。将来的には経験ログから記事素材を整理し、Bloggerへの記事作成・投稿へつなげる。

## 4. 現在の最重要テーマ

### VAIO P

- Debian 12 i686 / Intel Atom Z520 / 2GB RAM。
- システム軽量化は完了。追加軽量化へ戻らない。
- Chromium 152.0.7977.75は `--disable-gpu` で起動可能。日本語入力・スクロール・通常ページ操作を確認済み。
- バッテリー不調・発熱があるため必要時のみ起動し、終了後は停止する。
- Git 2.39.5導入済み。`/home/taka/ai-project` のローカルclone確認済み。
- VAIO Pでは手入力が困難。長文コマンド・長いJavaScript・細かなConsole操作を手入力前提にしない。可能な限り自動化・既存ファイル・最小操作を優先する。

### DIGA / DiMORA自動化

技術的目的は、VAIO P + Chromiumで認証済みDiMORAページから機械的にデータを取得できる経路を1つ再現可能な形で確立すること。

確認済み：Windows + Chromeの認証済みDiMORAでは `GL_FAVPGM_DATA.record[]` を確認。「もっと見る」はページ内 `record[]` の追加表示。VAIO P + Firefox ESRでは `GL_FAVPG_DATA` が見つからなかった。Firefox ESRでの長いJavaScript手入力による細かなConsole調査は非効率として打ち切り、同じ方法を繰り返さない。家PCを現在のDiMORA検証環境にはしない。

### 一次記録検索

- `scripts/search_direct_chat.py` はVAIO P上で実行できた。
- 初回検索では直チャット内容を出力した。
- 再実行では `403 rate limit exceeded`。原因はGitHub Contents APIで多数のファイルを個別取得する実装による未認証レート制限と判断。
- 同じAPI方式を通常ルートとして繰り返さない。ローカルclone＋`git grep` / `rg` を第一候補とする。
- 検索失敗はデータ不存在の証拠ではない。別経路で到達可能性を確認し、既知事項を理由なく再調査しない。

## 5. 現在の工程と次の一手

### 5-1. 現在レーン：ai-project運用基盤の大手術

2026-09-15、たかはClaudeの提案をできるだけ実施する方向を採用し、旧連番方式と新タイムスタンプ方式の混在を許容し、運用全体を大きく改修する方針を確定した。

順序：**現状棚卸し → 大手術の設計 → 採用する改善を確定 → 一括改修 → 検証**。

- 現状棚卸し：完了。
- 大手術の設計：完了。
- 採用する改善：確定。
- 一括改修：実施中。
- 検証：未完了。

採用：現在状態の構造化、一次記録と現在ビューの分離、BUDダッシュボード化、引き継ぎの現在ビュー化、新規直チャットのタイムスタンプ方式、旧連番維持、ローカル検索優先、秘密情報検査、命名・重複・リンク・状態整合性のCI検査、機械判定可能なルールのスクリプト化、Actions/Issuesの必要最小限の活用。

本来のDiMORA工程は大手術完了後に再開する。DiMORA完了条件は、認証済みDiMORA上で取得経路を実証し、再現可能性を確認すること。

### 5-2. トラブル対策

VAIO Pローカルclone、`scripts/search_direct_chat.py`、`rules/再開・検索ガード.md` は予備ルート。トラブル対策を本来の次の一手として残さない。

## 6. 経験DB・一次記録

- 直チャット・EXP・RECを中心に経験を蓄積する。
- ID管理の正本：`指針等/ID管理台帳.md`
- 経験DBの確定仕様：`docs/2026-09-05_企画書_経験DB運用仕様_確定版.md`
- EXP-0000000030の履歴調査ではREC17件中14件の一次記録が未特定。未追跡を追跡済みとは扱わない。
- EXP-0000000060 / REC-0000000010：VAIO P上の直チャット検索とGitHub APIレート制限。
- EXP-0000000070 / REC-0000000011：再開時の状態同期不全・既知事項再調査による作業停滞の運用点検。

### 直チャット

- 通常の会話は保存しない。明示的な保存指示だけ保存する。
- 保存対象・保存後処理の正本は `rules/運用ルール.md`。
- 2026-09-15の直近保存済み記録は **024**。実体は `直チャット/2026-09-15_直チャット即時保存_024.md`。
- 旧連番方式と新方式の混在を許容し、旧連番ファイルは変更せず歴史資料として保持する。
- 新規保存はタイムスタンプ方式へ移行可能とする。
- 保存後は現在資料へ最新保存情報を同期し、読み戻して一致を確認する。

## 7. 構造整理・再開点の原則

現在の正本は `BUD.md` と `rules/運用ルール.md` を中心とする。補助・歴史資料は役割がある限り残す。

直前セッションで確定した現在の次の一手は、会話だけに残さずBUDと現在の引き継ぎへ反映する。次回起動時にBUD・引き継ぎ・実際の最新直チャット間で不一致があれば、具体操作へ進まず不一致を解消する。次の一手と具体的操作を同一視せず、確定できない操作を推測しない。

## 8. 大手術の記録

- `docs/現在状態.json`：構造化された現在状態。
- `docs/大手術設計_2026-09-15.md`：棚卸し・設計・採用改善・完了条件。
- 2026-09-15の023保存後、BUD・現在の引き継ぎへの同期を実行し、読み戻し確認を行った。その後024を保存したため、今回さらに024へ同期した。
- 023同期が前ターンで未完了だったのは、保存後処理を途中で止めたバド側の処理漏れ。今回是正済み。

## 9. 安全・判断原則

- 認証情報・セッション情報・Cookie等の秘密情報を保存しない。
- 過去の計画を現在の命令として再実行しない。
- 外部提案は事実／提案を分離 → バド自身の採否判断 → 理由・懸念 → 次の行動 → 実行 → 検証。
- 完了時は確認済み・変更・未完了・保留・次の一手を明確にする。
- 終了時に確定した現在状態・次の一手はBUDと引き継ぎへ同期してからクローズする。

## 10. 今このファイルを読んだAIが答えられるべきこと

**目的**：経験を蓄積・再利用できるAI協働基盤を作る。

**現在の技術目的**：VAIO P + Chromiumで認証済みDiMORAから機械可読データを取得する再現可能な経路を1つ確立する。

**現在の直近の一手**：大手術の残りを一括改修し、最後にBUD・現在の引き継ぎ・構造化現在状態・一次記録・ルールの整合性を読み戻して検証する。DiMORA本来工程は運用基盤改修後に再開する。

**ルールの正本**：`rules/運用ルール.md`。
