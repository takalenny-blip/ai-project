# BUD — バドのための最上位コンテキスト

更新日：2026-09-10

## 0. このファイルの目的

このファイルは、`ai-project` を初めて読むAI、特にバドが、このプロジェクトの**現在の目的・現在状態・現在の判断基準**を短時間で再構成するための最上位資料である。

**最初にこのファイルを読むこと。**

作業の進め方・保存・履歴調査等の手順は `rules/運用ルール.md` に一本化する。`docs/AI_READ_MAP.md` は読解上の補助マップ、`rules/README.md` はルール入口として扱う。

過去資料は歴史資料であり、そこから現在の役割・ルール・次の作業を勝手に推定しない。

## 1. この活動の中心

- **たか**：この活動の中心。実際に経験し、試し、使い、判断し、最終的に決める。
- **バド**：たかと協働するAI。検索・整理、設計、分析、提案、文章化、実装、検証、経験資産化等を必要に応じて担う。
- **JUN / Claude / その他**：必要なときに使う補助手段。過去の担当実績は現在の固定役割・必須工程を意味しない。

**たか＋バドだけで完結できる場合、それで完結してよい。**

## 2. このリポジトリの目的

たかの実経験を、試行錯誤・成功・失敗・判断・気づきまで含めて蓄積し、AIが過去と現在をつなぎ、将来の自動化・記事化へ再利用できる状態を作る。

最終的には、経験ログから記事素材を発見・整理・文章化し、Bloggerへの記事作成・投稿につなげる。

## 3. 現在状態

### 3-1. 現在の最重要テーマ

**VAIO P運用環境**

VAIO Type Pは、常時起動の実運用基盤とは扱わない。バッテリー不調と発熱があるため、**必要なときだけ起動し、作業終了後は即停止する**。自動化の実験・補助端末として使う場合も、この運用条件を優先する。

**DIGA / DiMORA自動化**

DIGA録画管理を発展させ、特に「お気に入り・機種別制御・警告メール」を今後自動化する重要テーマとして進める。

現在までに、Windows + Chromeの認証済みDiMORAページでHTML内に `GL_FAVPGM_DATA` が存在し、`record[]` にお気に入りデータが保持されていることを確認した。また「もっと見る」は追加サーバー通信ではなく、ページ内の `record[]` を10件ずつ追加表示するJavaScript処理であることを確認した。

したがって現在は、**「認証済みDiMORAページを取得し、`GL_FAVPGM_DATA.record[]` を安全に読み出す方法を検証する」段階**である。

2026-09-08、現在使用している制限付きWindows PCではF12、Ctrl + Shift + I、Ctrl + U、ChromeメニューからのDevTools操作が利用できないことを確認した。この環境で同じ操作を反復せず、制限のない環境で検証する方針に切り替えた。

2026-09-10、VAIO P + Firefox ESRの認証済みDiMORAページで追加確認を行ったが、`GL_FAVPGM_DATA` はページソース・現在のDOM HTML・グローバル変数のいずれにも見つからなかった。`typeof GL_FAVPGM_DATA` は `undefined`、DOM HTML内検索は `-1`。script要素は34個で、`src`なしのscriptは27個だった。27個の数え上げまで確認したところで、長いJavaScriptをVAIO P上で手入力する方法は非効率と判断し、細かなConsole調査を打ち切った。VAIO PにはDiMORA調査のための変更を加えていない。

### 3-2. 現在の次の一手

2026-09-10時点の現在の次の一手は、**制限のないWindows PCを「家PC」と呼び、家PCでDiMORA調査を再開すること**である。

家PCは、DevTools操作について以前の制限付きWindows PCとは異なる環境であり、DiMORA調査の起点とする。ただし、**家PCで `Python.exe` を実行するとインストーラーが起動する問題が確認されているため、家PCを「何でもできる開発環境」とは扱わない**。Pythonの実行環境は未確定・要確認とし、利用可能と仮定して作業を進めない。

家PCでは、すでに `GL_FAVPGM_DATA.record[]` の存在を確認できているWindows + Chrome環境を起点に、制限付きWindows PCで以前使えなかったDevTools操作を繰り返さず、安全な取得方法を検討する。

VAIO P + Firefox ESRでの存在確認は現時点で未達のまま保留とし、長いJavaScriptの手入力による細かな確認は再開しない。

存在確認・取得方式・認証状態の扱いが確定するまでは、本番実装、定期実行、変更検知等へ先走らない。

過去REC・直チャット・旧docs・JUNレビュー等に書かれた「次にやること」は候補情報であり、現在の命令ではない。

### 3-3. 経験DB

直チャット・EXP・RECを中心に、実際の経験を後から検索・再利用できる形で蓄積する。

現在登録されている主なEXPは `EXP-0000000010`、`EXP-0000000020`、`EXP-0000000030`、`EXP-0000000040`。EXP-0000000030の最終RECは `REC-0000000190`、EXP-0000000040の最終RECは `REC-0000000020`。

ID管理は `指針等/ID管理台帳.md`、経験DBの確定仕様は `docs/2026-09-05_企画書_経験DB運用仕様_確定版.md` を参照する。

EXP-0000000030の履歴調査では、REC17件を全件列挙し、明示的な直チャット参照がある3件を確認、残る14件は未追跡としてCoverageに残した。防止ルールは検証済みだが、残る14件の一次記録探索は追加監査として未完了である。未追跡を追跡済みとは扱わない。

### 3-4. 直チャット

直チャットは一次記録。2026-09-09に**1ターン1ファイルで即時保存する方式**を確定した。

保存に関する現在の実行手順は `rules/運用ルール.md` の「運用フロー（直チャット保存）」に一本化する。特に、各ターンについて**送信する最終本文を原文として保存し、GitHub読み戻し後に「存在確認」と「原文忠実性確認」の二段階を通過するまで保存完了と扱わない**。

- `YYYY-MM-DD_直チャット即時保存_連番.md`
- 連番を会話順序の正本とする
- `[User]` / `[Bud]` の原文を保存する
- ユーザーが貼ったJUN・Claude等の回答も、省略・要約・再構成せず完全な提示原文として同じターンに保存する
- 保存後はGitHubから読み戻して、存在と原文忠実性を確認する
- 保存に失敗・不一致があれば保存完了と扱わず、原因を確認して修正・再確認する
- 原文を取得できない場合は推測・再構成しない
- 添付ファイル・画像の読み取りでは、`表示文字`、`画像内容`、`読み取り本文`、`バドの解釈`、`不明／判読不能`を分離する
- `直チャット/README.md` は管理・索引用で、毎ターン更新しない

2026-09-10のセッションでは、音声添付の記録・JUN提示の文字起こし・バドによる内容評価を `0035`～`0037` に保存・読み戻し確認した。続いてJUNに、2本の音声内容と現在の `BUD.md` / `rules/運用ルール.md` の整合性を確認してもらい、現在のプロジェクト定義・運用ルールを変更する実質的な差分はないとの第三者的バリデーションを得た。この確認結果と、たかとの追加確認を含む直チャットは `0039` まで保存・読み戻し確認済みである。`0040` は今回のクローズターンとして保存する。

`0010` は保存処理が失敗した記録があり、欠番を推測で埋めない。`0017` は以前の保存内容に原文忠実性の問題が確認されているため、旧保存を無条件に正本とは扱わない。

今回の2本の音声について、JUN提示の文字起こしから直接確認できた設計上の提案は、VAIO PをAIの「手足／エッジノード」として使うこと、およびBUD.mdを現在の目的・状態・次の一手の復旧中心とし、過去ログの参照範囲を絞ること。これらは現在のBUD方針と整合するため採用方向とした。ただし、現在の技術上の次の一手は変更せず、Firefox ESR上でのDiMORAデータ存在確認を先行する。JUNの追加分析に含まれた、音声に直接出ていない具体策は音声由来の事実として採用しない。

今回のJUNによる7項目の整合性確認でも、現在の `BUD.md` / `rules/運用ルール.md` に実質的な変更差分はないと確認された。したがって、コンテキスト隔離／「保存は豊かに・通常参照は絞る」は既存方針の第三者的な裏付けとして扱い、運用ルールの追加変更は行わない。VAIO Pのエッジノード化は既存方針どおり継続し、経験ログAI下書き・ブログ編集レイヤー・ディレクトリ大改造は現時点では保留または不要とする。

2026-09-10の直チャット即時保存は `0050` 以降も継続し、`0063`、`0064`までGitHub保存・読み戻し確認済み。0064では、VAIO Pでの細かなConsole調査を打ち切り、制限のないWindows PCを「家PC」と呼んでDiMORA調査を再開する判断を記録した。0065ではVAIO Pを常時起動しない現在条件を記録し、0066では家PCのPython.exe実行時にインストーラーが起動する問題を記録した。

## 4. 現在の運用原則

### 現在化

**過去ログの「次にやること」は、現在の「次の一手」ではない。**

作業開始前に、現在資料・引き継ぎ・実績を照合し、次を区別する。

- 現在の事実
- 過去の判断
- 過去の計画
- 完了
- 失敗／破棄
- 再試行条件
- 現在有効なルール
- 現在の次の一手

未完了／完了だけで判断せず、「当時未完了だが現在は不要」「失敗だが条件変更で再試行可能」「必要だが優先度が低く保留」を区別する。

### 失敗・環境差

同じ環境・同じ条件・同じ方法で既知の失敗を無条件に繰り返さない。環境・条件・新情報が変わった場合は、現在の目的との一致を確認して再検証する。

環境制約・権限・利用できなかった手段も、次の判断に影響する場合は現在状態の一部として引き継ぐ。

### 記録

作業上の事実は可能な限りテキストで残す。画像は補助記録であり、重要な事実を画像だけに依存しない。過去資料を現在の事実へ書き換えず、事実と推測を混同しない。

## 5. 重要な技術上の未確定事項

- 家PCで安全にDiMORAデータを取得する具体的方法
- 認証状態を安全に扱う方法
- 家PCで確立した取得処理をVAIO Pへ移す具体的方法
- VAIO P + Firefox ESRで `GL_FAVPGM_DATA.record[]` が存在するか（現時点では未確認のまま保留）
- 定期実行、変更検知、機種別制御、警告メールまで含めた現行仕様
- Firefox ESR / VAIO Pでの性能・安定性
- 家PCで `Python.exe` を実行するとインストーラーが起動する問題の原因と、Python実行環境として利用可能かどうか

## 6. 現在避ける手戻り

- 制限付きPCでF12、Ctrl + Shift + I、Ctrl + U、DevTools操作を再試行する
- Windows + Chromeの確認結果だけでFirefox ESRでも同じと推測する
- VAIO Pで長いJavaScriptを手入力して細かな存在確認を延々と続ける
- 家PCでの取得方式が決まる前にChrome Remote Debuggingを本番方式として実装する
- 「もっと見る」の追加サーバー通信を前提に調査をやり直す
- 同じ根拠のままPythonバージョン確認を再実行する
- 過去ログの次回アクションを現在の命令として無条件に採用する
- 認証情報、Cookie、セッション情報、パスワード、APIキー等をチャットやGitHubへ保存する

## 7. 読解と作業の入口

1. `BUD.md` — 現在の目的・状態・次の一手
2. `docs/AI_READ_MAP.md` — 読解上の補助マップ
3. `rules/運用ルール.md` — 現在の作業手順・運用ルール。直チャット保存を含む現在ルールの正本
4. `docs/引き継ぎ/現在の引き継ぎ.md` — 直前セッションの再開情報
5. `projects/` — 関係するプロジェクト資料
6. 必要なEXP / REC / 直チャット
7. 過去資料 — 歴史確認が必要な場合のみ

通常時は必要な範囲だけ段階的に読む。網羅性が目的の履歴調査では `rules/運用ルール.md` の「運用フロー（過去記録調査）」に従う。

## 8. 過去資料との関係

2026-09-05以前の「3者の役割」「JUN向け」「Claudeの担当」等の資料は、その時点の歴史資料として保持する。現在の固定役割・必須工程として扱わない。

過去を削除して歴史を書き換えるのではなく、現在の認識をこのBUD.mdと現在の運用ルールで明確にする。

## 9. BUDと運用ルールの役割分担

- **BUD.md**：現在の目的・現在状態・現在の次の一手・重要な判断基準を集約する。
- **rules/運用ルール.md**：どう動くかを定義する。実際の手順は「運用フロー」、横断的な制約は「ルール」、禁止事項は「禁止事項」として区別する。直チャット保存を含む現在の運用ルールの正本とする。
- **AI_READ_MAP / README**：入口・案内。現在情報の正本を分散させない。
