# BUD — バドのための最上位ダッシュボード

更新日：2026-09-14

## 1. ここで最初に確認すること

このファイルは、`ai-project` の**現在の目的・現在状態・現在の次の一手**を短時間で把握するための最上位ダッシュボード。

- 作業手順・保存・履歴調査・EXP/REC・外部提案対応等の正本：`rules/運用ルール.md`
- 読解順の補助：`docs/AI_READ_MAP.md`
- `rules/README.md` は入口だけを示す補助資料
- 過去資料の「次にやること」は現在の命令ではない。現在資料と実績で現在化してから判断する。

## 2. 活動の中心

- **たか**：実際に試し、使い、経験し、最終判断する中心。
- **バド**：たかと協働し、調査・整理・設計・分析・提案・実装・検証・経験資産化を担う。
- **JUN / Claude / その他**：必要時に使う補助手段。提案は事実と意見を分け、バド自身の採否判断を明示してから扱う。

## 3. リポジトリの目的

たかの実経験を、成功・失敗・判断・気づきを含む一次記録として蓄積し、AIが過去と現在をつなげて再利用できる状態を作る。将来的には経験ログから記事素材を整理し、Bloggerへの記事作成・投稿へつなげる。

## 4. 現在の最重要テーマ

### VAIO P

- Debian 12 i686 / Intel Atom Z520 / 2GB RAMの軽量環境。
- システム軽量化は完了。追加軽量化へ戻らない。
- Chromium 152.0.7977.75は `--disable-gpu` で起動可能。日本語入力・スクロール・通常ページ操作を確認済み。
- バッテリー不調・発熱があるため、必要時のみ起動し、作業終了後は停止する。

### DIGA / DiMORA自動化

現在の技術的な目的は、**VAIO P + Chromiumで、認証済みDiMORAページから機械的にデータを取得できる経路を1つ再現可能な形で確立すること**。

確認済みの基準情報：
- Windows + Chromeの認証済みDiMORAでは `GL_FAVPGM_DATA.record[]` を確認済み。
- 「もっと見る」はページ内 `record[]` の追加表示であることを確認済み。
- VAIO P + Firefox ESRでは `GL_FAVPGM_DATA` が見つからなかった。
- Firefox ESRでの長いJavaScript手入力による細かなConsole調査は非効率として打ち切り、同じ方法を繰り返さない。
- 家PCをDiMORA取得方式の現在の検証環境にはしない。

## 5. 現在の次の一手

**VAIO P + Chromiumで、認証済みDiMORAページから機械可読データを取得する短く再現可能な方法を1つ検証する。**

条件：
- `GL_FAVPGM_DATA.record[]` と同一であることは前提にしない。等価な機械可読経路ならよい。
- 認証状態の扱いを確認する。
- 存在確認・取得方式・認証状態が確定するまでは、本番実装・定期実行・変更検知へ先走らない。
- 長い手入力や、既知の失敗条件の反復はしない。

## 6. 経験DB・一次記録

- 直チャット・EXP・RECを中心に経験を蓄積する。
- ID管理の正本：`指針等/ID管理台帳.md`
- 経験DBの確定仕様：`docs/2026-09-05_企画書_経験DB運用仕様_確定版.md`
- EXP-0000000030の履歴調査では、REC17件中14件の一次記録が未特定のまま。未追跡を追跡済みとは扱わない。

### 直チャット

- 通常の会話は保存しない。
- **たかが明示的に保存を指示した場合だけ保存する。**
- 保存対象・保存後処理の正本は `rules/運用ルール.md`。
- 対象を実際に確認できない場合は推測・再構成しない。

## 7. 構造整理の方針

現在の正本は、次の2本を中心とする。

1. `BUD.md`：現在の目的・状態・次の一手
2. `rules/運用ルール.md`：現在の行動手順・保存・判断ゲート

補助・歴史資料は、役割がある限り残す。**ファイル数だけを理由に削除しない。**一方、同じ現在情報を重複して持つファイルや、役割を失ったファイルは、確認後に削除・統合してよい。

現在、`rules/README.md` と `docs/AI_READ_MAP.md` は入口・読解順に役割を限定済み。`docs/全体進行設計図.md`、`docs/引き継ぎ/現在の引き継ぎ.md`、EXP/REC、直チャット等は、それぞれ全体構造・セッションスナップショット・一次記録／履歴として役割があるため、理由なく削除しない。

## 8. 安全・判断原則

- 認証情報・セッション情報・Cookie等の秘密情報を保存しない。
- 過去の計画を現在の命令として再実行しない。
- 外部提案は、事実／提案を分離 → バド自身の採否判断 → 理由・懸念 → 次の行動 → 実行 → 検証、の順で扱う。
- 完了時は、確認済み・変更・未完了・保留・次の一手を明確にする。

## 9. 今このファイルを読んだAIが答えられるべきこと

**目的**：経験を蓄積・再利用できるAI協働基盤を作る。

**現在の技術目的**：VAIO P + Chromiumで認証済みDiMORAから機械可読データを取得する再現可能な経路を1つ確立する。

**次の一手**：短く再現可能な取得方法を検証し、認証状態と取得結果を確認する。

**ルールの正本**：`rules/運用ルール.md`。
