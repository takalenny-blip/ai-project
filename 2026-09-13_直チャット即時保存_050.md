# 2026-09-13 直チャット即時保存 050

## 対象
直前のBudの返答のみ。

## Bud
ある。さっきは保存を優先して、見解を返さなかった。そこは俺のミス。

今回の表示から見ると、

- **Chromium本体は起動できている** → `launch-light.sh` 自体は機能している。
- `network service crashed ... restarting service` → Chromium内部の **Network Service が一度落ちて、自動再起動している**。
- `registration response error ... deprecated_endpoint` → 登録処理の通信で、**古い／廃止予定のエンドポイントに関するエラー**が出ている可能性が高い。
- したがって、現時点では **「Chromiumが起動不能」という問題ではない**。
- ただし、Network Service のクラッシュは無視してよいと即断もできない。

そして重要なのは、今回の起動では **`--disable-gpu` を付けた状態**だということ。

だから、今は設定を増やさない。  
**次はChromiumで普通にWebページを1枚開いて、表示・スクロール・日本語入力が正常か確認する。**

エラーそのものをさらに特定するのは、その後でいい。
