# 直チャット即時保存

## User

家pcの作業は削除していい。その方針で進めてくれ。やりとり書き込んで

## Bud

了解。家PCでのDiMORA作業は現在方針から外す。

BUD・全体進行設計図・現在の引き継ぎ・`projects/DIGA自動化.md` を更新し、現在の検証環境を **VAIO P + Chromium** に統一する。家PCで過去に確認したPython環境等は履歴として残すが、現在の作業手順には含めない。

現在の次の一手は、VAIO P + Chromiumで認証済みDiMORAページから、機械的に読み出せるデータ取得経路を1つ再現可能な形で確立すること。`GL_FAVPGM_DATA.record[]` は比較対象として使い、同一オブジェクトに限定しない。VAIO P + Firefox ESRで行った長いJavaScript手入力の調査は繰り返さない。

今回の方針変更はGitHubへ保存し、各資料を読み戻して整合性を確認する。今回の方針変更自体は新規EXP/RECを推測で発行せず、既存記録との所属関係は根拠確認後に判断する。
